# MPQUIC — Codex locale

Per task non banali usa il `tech-lead` locale. Le fonti canoniche degli agenti restano in `.claude/agents/`; consulta `docs/WORKING_INSTRUCTIONS.md` e la documentazione MPQUIC/ROMARS pertinente. Mantieni locali stack Go/OpenWrt, trasporto multipath, STRIPES, procedure di deploy ed ECSS del progetto. Si applica inoltre l'`AGENTS.md` globale del workspace.
