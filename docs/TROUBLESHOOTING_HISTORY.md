# Troubleshooting History — MPQUIC

Log operativo delle diagnosi di troubleshooting sul progetto MPQUIC (server VPS, client VM/OpenWrt, data plane STRIPES). Una entry per sessione, dalla più recente alla più vecchia. È la fonte grezza: le lezioni riusabili che se ne ricavano vengono distillate nella wiki LLM condivisa (`/opt/TPZ/src/wiki/`) e linkate da qui.

Quando arriva un caso simile, prima si cerca qui se è già stato visto.

## Come si usa

Per ogni nuovo troubleshooting si aggiunge una entry in cima all'indice e al corpo, con questo template:

```
## TS-NNN — <target> — <titolo sintetico>

- **Target**: <VM/host/nave> (<hostname>) — <IP> — <accesso>
- **Data**: YYYY-MM-DD
- **Sintomo**: <cosa lamentava l'utente/cliente>
- **Diagnosi**: <cosa è stato verificato, read-only, con evidenze>
- **Evidenze**: <numeri, output networkctl/lease/contatori kernel>
- **Causa radice**: <dove si rompe davvero la catena>
- **Fix proposte**: <opzioni, con impatto/rischio>
- **Esito / stato**: <cosa è stato deciso e fatto; aperto/chiuso>
- **Lezione → wiki**: <link alla lezione distillata, se presente>
```

Regole: diagnosi sempre read-only prima di proporre; nessuna modifica su VM di terzi o di produzione senza conferma esplicita dell'utente; le evidenze numeriche vanno riportate, non riassunte a memoria.

## Indice

| ID | Data | Target | Sintomo | Stato |
|----|------|--------|---------|-------|
| TS-034 | 2026-08-30 | Nuova TBOX-EVO (Proxmox `TBOX-10241438` 10.10.10.2, OpenWrt VM 100 10.10.11.254, VM 200 `MPQUIC` 10.10.11.100) + VPS lab 104.105.82.146 | TBOX-EVO bruciata senza backup, ricostruita da un clone vecchio ~2 mesi: OpenWrt senza `STARLINK_PHY`/`LTE_PHY`; VM senza VLAN phy/tabelle/pavimenti/foreign-rules, script `126fbbc9` senza `phy_table`, unit vecchie, binario CAL di giugno (`418d7b6`), VPS sbagliata (172.232.209.51), `enp7s8` statica 10.10.99.2/29 da drop-in NQX e `net13` su `vmbr0`, istanze 1-6 ancora a `/30` peer `.2`. Riferimento IBLEA-M letto in sola lettura; VPS lab con binario da `98f0fb9` dirty e crypto `hybrid_security` | **Chiuso 🟢 sul banco**: build pulita `98f0fb9` (md5 `d67f8c82`) su VPS e client, `net13`→`vmbr7`, drop-in NQX via, networkd/rt_tables/script/unit dal repo, `mp1.yaml` dual-WAN + tuning IBLEA v9 + sezione crypto, istanze 1-6 a `/24` (TS-020), OpenWrt PHY = copia IBLEA-M in BONDING+FAILOVER m6/m7. Tre scoperte in corsa: cavo Starlink su `vmbr3` (spostato dall'utente su `enp9s0`), `/30` (watchdog-peer-fail, `mpq6` riavviato ogni 17 s), e **`mpquic-vps-routes.sh` vecchio hard-coded (`ea851b31`) eseguito sul client dall'`ExecStartPost`**: `172.16.6.0/30 dev mpq6` in `main`, risposte all'OpenWrt rispedite nel tunnel, `STARLINK` mai online → script canonico `3e7e3492`. Finale: mp1 12 pipe, peer 0% 35 ms; VLAN 96 0% 35 ms; `mwan3 use` STARLINK/STARLINK_PHY/BOND1 5/5 0% 29-34 ms; **STARLINK, STARLINK_PHY, BOND1 online**, LTE_PHY offline veritiero (LTE senza cavo). **Residuo produzione**: IBLEA-M ha lo stesso script vecchio → `172.16.3.0/30 dev mpq3`, `172.16.4.0/30 dev mpq4` stantie in `main` (oggi innocue: VSAT su 172.30.229.x fisica), da sanare in finestra con ok umano |
| TS-033 | 2026-08-07 | Banco TBOX-EVO (uplink fisico Starlink `enp9s0` su Proxmox, OpenWrt VM 100, VM 200 lab) | Dopo stacco/riattacco mattutino della scheda di rete (10:56→11:00), `STARLINK_PHY` non tornava più online in mwan3 (e `STARLINK` restava in connecting) per tutta la giornata; nel frattempo swap VM 300→VM 200 (~16:19) e riavvio OpenWrt (~16:22). Diagnosi: ping manuali 20/20 a 0% (sorgente `172.16.96.2`, avg 65 ms) ma tracker mwan3 con successi alternati a "Lost 4 ping(s)" — perdita a raffiche; su dmesg dell'host Proxmox `enp9s0` (bridge `vmbr7` = Starlink fisica, VM200 `enp7s8`) in bounce anche alle 16:27:49 e 16:28:31; con `up=5` il tracker non somma mai 5 successi consecutivi → connecting perenne su path che nei burst funziona | **Chiuso 🟢 (rientrato da solo)**: ultimo bounce 16:28:35, `STARLINK_PHY` online alle 16:29:16 (score 0→5 in 25 s), `STARLINK` online, stabile a +6 min, contatori NIC puliti (0 err su 15,3M pkt, 1000/Full). VM 200 sana (lease `9.246.8.61/23`, default `phy6` via `9.246.8.1` sopra il pavimento). Coda: se `enp9s0` rifà bounce fuori dalla finestra notturna ~03:50 (riavvio noto del router Starlink), verificare connettore/porta riattaccata; occhio anche a TELESAT (`eth9`) che perdeva check negli stessi minuti |
| TS-032 | 2026-08-06 | TBOX-EVO — VM 300 `MPQ-ROMARS` (10.10.11.100 / ZT 10.202.15.2:3222) | Difetto A11 del prontuario ROMARS: VLAN 95/96 dei canali fisici assenti lato VM 300 — da OpenWrt ARP `INCOMPLETE` su `172.16.95.1`/`172.16.96.1`, membri mwan3 `LTE_PHY`/`STARLINK_PHY` offline, failover fisico inesistente. ROMARS ha autorizzato Telespazio a intervenire con perimetro stretto: **solo collegamento delle subnet** delle due interfacce OpenWrt a interfacce VM che escano su `enp7s7` (LTE) e `enp7s8` (Starlink), **zero modifiche al software** | **Chiuso 🟢 in giornata, config-only**: 4 file networkd (VLAN 95/96 + tabelle phy5/phy6 con rule 1095/1096 networkd-owned e pavimento blackhole), righe `VLAN=` nei padre, `rt_tables.d/mpquic-phy.conf`, e — al posto del backport `phyN` nello script, vietato dal vincolo — **drop-in `[Route] Gateway=_dhcp4 Table=195/196`** su 14-wan5/15-wan6: il default dinamico lo mantiene networkd stesso. Collaudo: VM ping sorgente 0% (LTE 60,8 ms, Starlink 18,0 ms), `route get ... iif enp7s2.96` risolve in `phy6` (le regole ROMARS 999/1000 non intercettano le VLAN figlie); da OpenWrt 20/20 a 0% su entrambe le sorgenti PHY e **`LTE_PHY`/`STARLINK_PHY` online in mwan3**. Script ROMARS-side invariato (md5 `21856433` confermato post-intervento). Snapshot `pre-phy-vlan-20260806` |
| TS-031 | 2026-07-29 | Banco TBOX-EVO (trasporto STRIPES mp1, client VM200 + VPS lab) | Mandato utente (loop): upload via mp1 pessimo ovunque — IBLEAM 22.7 Mbit con crollo a ~0 e 2180 retx contro 342 del fisico, banco 25.5 con 5975 retx contro 53-103 del fisico — mentre il download -P30 va bene. Baseline strumentata sul banco: **il trasporto non perdeva nulla** (33.150 pacchetti inviati = 33.152 ricevuti, NACK ~0, adaptive_m=0): le migliaia di retx TCP erano tutte **spurie da riordino**. Tre cause: (1) TX client mai pacato (`stripe_pacing_rate` assente: batch GSO a line-rate virtio dentro uplink ~50 Mbit = burst loss); (2) `selectBestPath` alterna il path **a ogni pacchetto** a parità di punteggio (riordino cross-path, RTT 29 vs 67 ms); (3) il pacing kernel, anche se configurato, era **inerte: NIC con `fq_codel`, mai `sch_fq`** (prerequisito scritto nel codice e mai applicato) | **Riaperto dall'utente (falso successo: validazione col metodo sbagliato) e richiuso il pomeriggio con due cause vere in più trovate con la review del transport-expert**: (1) debito EDT illimitato → drop silenziosi in sch_fq (479 dropped + 95 horizon_drops) → clamp orizzonte 15ms; (2) segnale peerLossRate rotto lato server (12 keepalive consumano la stessa finestra) → AIMD dietro flag, off; (3) esenzione pacing solo per ACK puri con byte addebitati; (4) **FEC adaptive = cricchetto tossico su downlink bursty** (A/B/C: fisico stabile, tunnel-adaptive decade 256→132→79, tunnel-FEC-off stabile 235-277) → `stripe_fec_mode: off` su mp1. Finale col metodo utente (OpenWrt→IP pubblico, back-to-back + soak 30s): **down 232 medi (target 200), up 65 medi (target 50), zero sessioni a 0**. v9 = commit `6429992`, binario `60965c62`. Aperti: C2 server per riattivare AIMD, maxOOO vs seq NACKati, metriche EDT |
| TS-029 | 2026-07-29 | IBLEA-M produzione (client VM200 + OpenWrt via tbox) | Segnalazione utente: in mwan3 `STARLINK` e `LTE` (membri-tunnel) sempre offline mentre i PHY sono online — percepito come guasto. Diagnosi: `STARLINK` offline è **strutturale** (traccia dentro `mpq6`, mascherata per scelta in TS-027, wan6 fail-closed; la Starlink vera viaggia in BOND1/mp1 + STARLINK_PHY); `LTE` offline era **veritiero** (uplink LTE di bordo di nuovo nero: carrier+lease+ARP ok, internet 100% loss anche sul fisico nudo — flapping dalla sera prima); il "verde" storico di quei membri era il leak in main chiuso col passo 3. Problema reale: rosso permanente by-design = alarm fatigue. Decisione umana: **eliminare i membri STARLINK/LTE** (interface+mwan3+firewall, lato tbox) — set finale `BOND1, VSAT1, VSAT2, STARLINK_PHY, LTE_PHY` | **Chiuso 🟢 dai due lati** (stessa mattina): lato mpquic `mpquic@5` stop+disable+mask (come mpq6), wan5 fail-closed, mp1/mpq3/mpq4 verificati; lato tbox rimozione STARLINK/LTE da network+mwan3+firewall senza orfani, VLAN fisiche salve, tracking VSAT rilassato su suggerimento mpquic (timeout=3 count=3) → **VSAT2 recuperato online**. Contro-verifica finale: BOND1+VSAT1+VSAT2+STARLINK_PHY online, LTE_PHY offline veritiero (uplink giù), 12 istanze attive 0 failed, mp1 ~44 ms 0%. Cruscotto ora veritiero: niente più rossi strutturali. Facoltativo futuro: ritiro WAN1/WAN2 (offline oneste), pulizia /30 client e istanze server 5/6 sul VPS |
| TS-028 | 2026-07-29 | IBLEA-M produzione (client VM200) | Su mandato umano: reboot completo della VM200 (al posto del restart networkd pendente da TS-027) come collaudo di persistenza, più chiusura della parte (b): rimozione del default fisico da `main`. Pre-flight: via d'emergenza `ssh -p 2222` verificata, **drift di boot trovato e sanato** (famiglia `*4` attiva ma `disabled`: al riavvio sarebbe sparita in silenzio). Reboot: VM su in ~1 min, 13/13 istanze da sole, rule tutte `proto static`, phy popolate, drop-in foreign-rules attivo, LTE tornato (phy5 0% loss). Parte (b): `gw_for_dev` esteso ai lease networkd (`/run/systemd/netif/leases`, chiave `ROUTER=`), poi `UseGateway=false` su wan5/wan6 e default fisici rimossi da `main`. Gotcha: la chiave vive solo in `[DHCPv4]`, nella legacy `[DHCP]` systemd 252 la ignora ("Unknown key") — primo tentativo silenziosamente inefficace, scoperto solo dal renew-test. Coda: i renew forzati hanno ucciso i binding CGNAT delle pipe mp1 (70% loss deterministico, journal muto, SSH nel tunnel fluida: firma TS-015) → bounce mp1 | **Chiuso 🟢** (commit `0a19b60`): renew-test passato (il default non torna, phy fresche dal lease), mp1 20/20 0% post-bounce, dead-man mai scattati. Il bypass implicito TS-017 non esiste più in nessuna forma; diagnostica per-device cambiata: niente più `ping -I enp7s8` nudo, il floor fisico si misura dalla sorgente VLAN (`-I 172.16.96.1`) |
| TS-027 | 2026-07-29 | IBLEA-M produzione (client VM200 + VPS) | Chiusura dei residui lasciati aperti da TS-025/TS-026, con tre scoperte durante il lavoro. (1) `mpq4` in reconnect continuo con `sendmsg: operation not permitted`, 18 eventi in 2 minuti e 81% di loss ICMP nel tunnel: a ogni "Configuring with ..." di networkd (rinnovo DHCP dei GEO, uno ogni pochi minuti) sparivano le `ip rule` 1201-1206 e le pipe restavano senza percorso, la malattia TS-021 mai chiusa in produzione. (2) Il pacchetto v5.1 di TS-025 non era mai entrato in esercizio: `/usr/local/bin/mpquic` era `cb5eb0d0` sul client e `4ac2bed3` (CAL) sul VPS, il v5.1 stava solo in `/opt/mpquic/bin/`. (3) Il ramo di produzione portava il codice giusto ma gli script operativi vecchi, e `mpquic-update.sh` installa le unit dal repo: il primo update ha rimesso un `mpquic@.service` senza `ExecStartPost` (return route di TS-012), e si è fermato a metà lista perché `mpquic@6` è mascherata (`set -e`) | **Chiuso.** Addressing `/24` peer `.254` su istanze 1-4 (client e VPS in coppia), wan rule 1001-1006 dichiarate a networkd + pavimento blackhole per tabella, timer di riconciliazione da 30s installato (non esisteva), hook `mpquic-if-event.sh` allineato, `mpquic-update.sh` reso tollerante alle unit mascherate. Deploy rifatto **dallo script su entrambi i lati**: stesso commit `3a31bf9`, md5 identico `63851a95`. Misure sul percorso cliente: `mpq3/4/5` da 60-81% a **0% loss**, mp1 single-stream download **21-39 Mbit**, upload 61.5, aggregato P10 **249 Mbit**. **Flow-affinity spenta su IBLEA** dopo A/B: con la flag attiva il download single-stream stava a 0.3-0.7 Mbit contro 73.5 del fisico misurato nello stesso minuto. Residuo: il drop-in `ManageForeignRoutingPolicyRules=no` è installato ma richiede un riavvio di `systemd-networkd` per attivarsi |
| TS-026 | 2026-07-28 | IBLEA-M produzione (client VM200) | Handoff tbox (loro TS-019): dopo la replica `.95/.96` su OpenWrt, i membri `STARLINK_PHY`/`LTE_PHY` restano offline in mwan3 perché su VM200 le tabelle `phy5`/`phy6` contengono solo il pavimento blackhole — manca il default via gateway fisico. Causa: il deploy v5.1 (TS-025) ha portato i file networkd delle VLAN phy ma NON lo script `mpquic-policy-routing.sh`, rimasto alla versione del 23/07 (md5 `99229f65`, zero occorrenze di `phy_table`): nessuno popolava il default dinamico. Fix: backport chirurgico del solo blocco phyN dal canonico del repo dentro lo script deployato (il canonico intero avrebbe rimosso il bypass TS-017 di wan6, vietato dal vincolo di sequenza dell'handoff), backup `.bak-ts019-20260728` | **Chiuso 🟢** in 3 fasi la stessa sera: (1) backport phyN → `STARLINK_PHY` online in mwan3, failover fisico funzionante; (2) **passo 3 in finestra autorizzata dall'umano**: canonico deployato (md5 `728e05ae`), bypass wan6 rimosso, pavimenti e rule ripristinati (erano state spazzate — malattia TS-021 ancora aperta in produzione), dead-man mai scattato; (3) coda ZT admin a 37% → return provato integro, causa = pipe CGNAT marcia sotto flow-affinity server (ARQ 13.5k NACK) + doppio artefatto burst-ping vs rate-limit ICMP → bounce mp1 client, rimisura tbox 0% loss, SSH fluida. Lezioni in wiki. Residui gated: rule wanN in networkd (sweep), rimozione default `main` (serve design gateway-detection) |
| TS-022 | 2026-07-27 | Banco TBOX-EVO (OpenWrt VM 100, LAN CORPORATE) | Traffico della LAN CORPORATE 192.168.222.0/29 che non va in forwarding: richieste viste `eth0 In` e sparite in silenzio, zero conntrack, zero ICMP di errore, DNS verso il router senza SYN-ACK. Firewall e mwan3 scagionati (config e runtime corretti, `route get` marcato risolveva su eth13). Colpevole provato per A/B: CoovaChilli spostato su `eth0` (per replicare il CP di IBLEA) intercetta in promiscuo i frame del suo `dhcpif` e inghiotte quelli dei MAC non autenticati PRIMA del netfilter: il laptop corporate era un utente captive non loggato. CP e LAN non possono convivere sulla stessa interfaccia | **Chiuso**: chilli riportato su `eth0.5` (decisione utente: unica divergenza dal CP IBLEA), CORPORATE di nuovo nei tunnel (giro completo eth0→eth13/mpq6→reply, 0% loss). Aggiunta regola fw "DNS LAN" (tcp/udp 53 da zona LAN, prima c'era solo DHCP e la zona è input=REJECT). Lezione: sparizione silenziosa pre-conntrack = cercare chi ruba i frame a L2 (captive portal/XDP/raw socket), non le regole L3 |
| TS-025 | 2026-07-28 | IBLEA-M produzione (client VM200 + VPS) | Cliente lamenta rallentamento. Deploy del pacchetto v5.1 in esercizio senza finestra di fermo: identità/VMID confermati via ZeroTier→Proxmox (`qm list`), snapshot `pre-v51-20260728`, via di emergenza indipendente dal tunnel trovata e verificata (`qm guest exec 200`). Applicati: VLAN fisiche .95/.96 + tabelle phy (additive, sbloccano tbox), rimozione del gateway fantasma `10.10.11.1` **regredito di nuovo** (TS-018), binario v5.1 su VPS e client, **flow-affinity attiva lato server su mp1**. mp1 riavviato con dead-man switch a 5 minuti (mai scattato). Scoperte: il traffico cliente di wan6 esce dal **bypass fisico** e `mpq5`/`mpq6` sono tunnel **QUIC semplici** (non STRIPES) sia su IBLEA sia sul banco → spostarci il traffico sarebbe un downgrade; il percorso "diretto" va sulle VLAN .95/.96. Il `git fetch` dal client è impraticabile sul link nave (timeout 300s): binario copiato compilato (15 MB) | **Chiuso lato mpquic**: single-stream downstream sul percorso cliente **33.8 Mbit** (era 0.3-20 ballerino), upload 39, aggregato P10 153 Mbit, peer ping 0%, 0 restart. Via libera dato a tbox nel registro. Aperti: addressing /30→/24 delle istanze 1-4/6, rimozione del bypass implicito in sincrono con i membri mwan3 di tbox |
| TS-024 | 2026-07-28 | Banco TBOX-EVO (trasporto STRIPES, mp1) | Fix notturno del single-stream: **flow-affinity per pipe** (hash FNV-1a del 5-tuple interno, flag `stripe_flow_affinity`, wire invariato) su baseline pre-CAL. Scoperta collaterale: affinity lato client uccide l'upload (flusso inchiodato su pipe con binding CGNAT degradato = 100% del danno, dove il RR ne assorbiva 1/12) → configurazione finale **asimmetrica**: affinity SOLO lato server (downstream, dove il riordino collassava TCP), client resta round-robin (upstream gia' sano col RR). Chiuso anche l'ultimo fall-through: pavimento blackhole su bd1 (durante il guasto lungo del soak BOND1 restava "online" finto scavalcando in main) | P1 -R inoltrato da pclab: **0.3-21 → 43-85 Mbit**; P30 -R 153-187 Mbit; upload tornato 13-21 Mbit. Branch `feat/flow-affinity-3c03829` (commit `4c469dc`), binario `72ad66c4` sui due lati, flag attivo solo nel mp1.yaml del VPS. Validazione 30 min sotto carico k6 in coda |
| TS-023 | 2026-07-27 | Banco TBOX-EVO (client VM 200 + VPS lab + OpenWrt) | Scoperta utente: i collaudi passavano dal canale fisico, non da STRIPES (IP pubblico di uscita = Starlink). Tre concause: bypass TS-017 con default attivo (`enp7s8`) innescabile da ogni run dello script fuori unit; host route /32 verso l'IP pubblico del VPS che dirotta sul fisico anche i test iperf; malfunzionamento della versione mpquic corrente (CAL/ML-KEM: mpq5 sparito, reconnect). Interventi: (1) bypass RIMOSSO dal client (solo blackhole, commit `93a8e29`, acid test ok); (2) VLAN dedicate ai canali fisici 95/96 stile BOND1 (tabelle phy5/phy6, rule 1095/1096 networkd-owned + floor blackhole; OpenWrt: LTE_PHY/STARLINK_PHY membri mwan3 di riserva m6/m7 in FAILOVER) — fallback fisico ora esplicito e governato da mwan3; (3) snapshot completo su branch `debug/anomalia-mpquic-20260727` (e6ddff7) e rollback binario a baseline pre-CAL `3c03829` (md5 `4c125555`) su client e VPS lab | **Baseline attiva e validata col metodo corretto** (iperf verso IP di TUNNEL): mp1 aggregato 118 Mbit in-tunnel, single-stream 0.65 Mbit (limite pre-reseq noto, TS-013), up 20 Mbit; mpq6 ~30/20 Mbit; peer ping 0%; pclab esce con l'IP del VPS. Famiglia 5 giù: il modem LTE non dà lease (fail-closed corretto). Aperto: debug della versione CAL sul branch; handoff tbox SOSPESO fino a ri-collaudo |
| TS-021 | 2026-07-27 | Banco TBOX-EVO dual-WAN (client VM 200 + OpenWrt VM 100) | Su OpenWrt tutte le WAN "Online" con le porte fisiche staccate; sul client le `ip rule` 1001-1006 sparivano da sole entro 2-3 minuti da ogni re-add → tutti i `/30` in bypass NATtato sul default fisico. Riprodotto e provato lo storm: `del`+`add` delle rule remote → EPERM sui socket QUIC → reconnect → il client ricrea il TUN → networkd riconfigura il link nuovo e (ManageForeignRoutingPolicyRules=yes di default) spazza le rule foreign → altri EPERM. In più networkd-dispatcher perde eventi di TUN appena creati ("Unknown index seen") → il default `dev mpqN` non tornava mai da solo | **Chiuso e validato sul banco** (rule stabili sotto trigger, fail-closed a tunnel giù, risalita ≤45s, `mwan3 use` 0% su LTE/STARLINK/BOND1, WAN morte offline). Wiki: candidata. Aperto: il client Go ricrea il TUN a ogni reconnect (amplificatore, valutare fix nel codice) |
| TS-020 | 2026-07-24 | Lab template (client VM 200 `10.10.11.100` + VPS lab `104.105.82.146`) | Bonifica del "template" mpquic prima della clonazione. Quattro cose in una sessione: (1) bypass routing: con un qualunque tunnel giù, tutto il traffico del suo `/30` usciva su `enp7s8` invece che in blackhole. Causa: `mpquic-policy-routing.sh` cancellava e riaggiungeva le `ip rule` statiche a ogni run, e il churn del watchdog (restart continuo di mpq1-5 morti) le lasciava sparite a metà finestra → tutte le WAN in bypass. (2) STARLINK flapping fermato con addressing `/30`→`/24` e peer `.254`. (3) IP di IBLEA `172.238.232.223` ereditato dal clone nel `global.env` della VPS lab → messo `104.105.82.146`; rimossi i `/run` stale col vecchio IP lab; `mp1.yaml` del repo hardcodava l'IP invece del placeholder. | **Chiuso e verificato** (trace `9.9.9.9`: STARLINK→mpq6 e BOND1→mp1, 0% loss, nessun bypass, rule stabili sotto churn). **Aperto**: failover mp1 `enp7s8`→`enp7s7` (scelta di design, rischio TS-016) |
| TS-019 | 2026-07-23 | TBOX-EVO lab (client mpquic 10.10.11.100) | Tunnel EVO che muoiono in continuazione (wan6 stripe timeout ~31s). Causa: EVO usava la VPS di IBLEA (`VPS_PUBLIC_IP=172.238.232.223` in global.env + IP hardcoded in mp1.yaml/mp1_RS_ADAPTIVE.yaml) invece della lab 172.232.209.51 → stesso IP enp7s8 (9.246.8.61) di IBLEA sulla stessa VPS → conflitto. Propagazione config IBLEA→EVO (sync oggi ~14:08, ctime .bak-ts018). Verificato inoltre: handshake ML-KEM NON è la causa dei guasti (test loss: X25519MLKEM768 = X25519, 15/15 al 30%). | **Fix VPS applicato** (global.env + mp1 → lab, 0 conn residue a IBLEA, protegge produzione IBLEA); **aperto**: verifica mapping/dataloss attende accensione VPS lab (spenta) |
| TS-018 | 2026-07-23 | IBLEA-M (client VM 200) | Dataloss ~40% sul traffico inoltrato da OpenWrt verso Internet. Causa (handoff tbox): default fantasma `10.10.11.1` (OPNsense dismessa) metric 0 da `01-mgmt1.network`, buco nero per tutto ciò che cade in `main`. Secondo difetto: STARLINK di OpenWrt esce su `eth8`=/30 `172.16.1.x`=`wan1` (enp7s3 morto), non su enp7s8. Fix: rimosso il gateway fantasma (persistente) + `wan1→enp7s8` diretto (runtime). Tunnel mp1/STRIPES misurato al 70% loss → MPQUIC non ripristinabile ora | **Chiuso lato client** (40%→7%, fisico 0%; residuo = install non standard: split Starlink + VLAN 1 flat). Passato a tbox per accessi TBOX + install |
| TS-016 | 2026-07-23 | IBLEA-M (data plane STRIPES, mp1 client+VPS) | Aggregato -P30 -R decade da 125 Mbit a ~40 Kbit in 17s (rischio penali). Causa: regressione dei commit post-374e3a1 (reseq 5e74c1e + f6c1d6b), provata con A/B binario known-good (167 Mbit -t30 vs collasso). Fix: git revert dei due commit, sync remoti via git bundle + mpquic-update.sh, build riproducibile (md5 identico al known-good) | **Chiuso** (validato -t30 entrambi i lati: 199 Mbit VM / 173 OpenWrt vs tetto fisico 294; target 266 da rimisurare col tetto diurno) |
| TS-015 | 2026-07-22 | IBLEA-M (client VM 200, config mp1) | Outage totale di bordo: mp1 al 50% loss deterministico perché la config deployata (in drift dal repo) stripava su 24 pipe di cui 12 su `enp7s7` (router LTE con lease DHCP+carrier ma upstream morto = blackhole). Aggravante: default mwan3 appena spostato tutto su BONDING→mp1, nessun failover. Fix: config mp1 single-WAN `enp7s8` + restart con dead-man switch + pulizia sessioni stale lato server | **Chiuso** (0% loss post-fix a link scarico; resta la variabilità Starlink sotto carico, tema TS-013) |
| TS-014 | 2026-07-22 | IBLEA-M (client VM 200, `mpquic-policy-routing.sh`) | `systemctl stop mpquic@6` provoca un blackout di 18.4s sulle pipe di mp1 (tunnel diverso, bindato su `enp7s8` come "ospite" di wan6). Causa: la route host verso il VPS in tabella wan6 era condizionata a `have_tun_up mpq6`, ma è una risorsa di WAN condivisa da più tunnel. Fix: sganciata la route host dallo stato del TUN, lasciata condizionata solo a `wan_usable`; il default/blackhole resta legato al TUN come prima | **Chiuso e validato sul campo** (deploy fatto; stop mpq6 post-fix: host route presente 38/38 campioni, max 1 FAIL su mp1 vs 14 pre-fix) |
| TS-013 | 2026-07-22 | IBLEA-M (client VM 200 + VPS, data plane STRIPES) | Throughput a pochi stream a fame (speedtest cliente 0.67/0.44 Mbps) mentre l'aggregato regge (-P30 ~240 Mbit). Causa: STRIPES distribuisce ogni pacchetto round-robin su N pipe senza reorder buffer al RX → il reordering arriva a TCP come perdita e fa collassare cwnd sul single-stream; sotto, loss variabile del downlink Starlink che il trasporto non recupera. Fix: reorder buffer al ricevitore (reseq) + kill-switch. Single-stream da ~73 Kbit a ~31 Mbit sostenuti; aggregato invariato | **Chiuso** (fix reale deployato fleet-wide; >200 single-stream su un solo link Starlink giudicato non realistico, l'aggregato resta la via per la banda piena) |
| TS-001 | 2026-07-16 | VM ROMARS (Proxmox ID 300) | Mancata detection di attacca/stacca cavi WAN e canale LTE che sembra online ma è un buco nero dopo lo scambio dei cavi. Cause: carrier VirtIO che non cade mai + lease DHCP LTE da 24h contro 5 min dello Starlink. Scoperto che `wan-watchdog.service` è già installato e abilitato sulla VM ma lo script manca (`203/EXEC` in crash loop dal boot) | Analisi chiusa, consegnata a ROMARS (attende applicazione lato fornitore) |

---

## TS-034 — Nuova TBOX-EVO — Ricostruzione dopo guasto hardware: canali fisici PHY copiati da IBLEA-M, VM riallineata al repo e alla VPS lab, tre difetti del clone trovati in corsa

- **Target**: nuova TBOX-EVO su hardware nuovo — Proxmox `TBOX-10241438` (PVE 9.2.3, `10.10.10.2`, raggiunto via jump `-J root@10.10.11.254` o DNAT `:2222`), OpenWrt VM 100 (`root@10.10.11.254`), VM 200 `MPQUIC` (`satcom@10.10.11.100`, machine-id `0e60d717…`, stessi MAC della vecchia VM 200), VM 500 `NQX-CLIENT` spenta. Management raggiunta da adattatore locale (`10.10.11.81`), ZeroTier del banco irraggiungibile. VMID verificato alla sorgente (`qm list`: 200 running, nessuna VM 300). Riferimento IBLEA-M (OpenWrt `root@10.202.11.2`, VM `-p 3222`) usato **in sola lettura**. VPS lab `104.105.82.146` (alias `vps-mpquic-it-tpz-lab`).
- **Data**: 2026-08-30
- **Sintomo**: TBOX-EVO bruciata senza backup; clone ricostruito da una versione vecchia di ~2 mesi. Mandato utente: replicare da IBLEA-M **solo** le due interfacce fisiche `STARLINK_PHY`/`LTE_PHY` (resto dell'OpenWrt invariato), ricopiare la configurazione mpquic di IBLEA-M sulla VM e puntarla alla VPS lab.
- **Diagnosi (ricognizione read-only sui tre livelli)**. OpenWrt: nessuna `_PHY`, nessuna VLAN 95/96, policy `BONDING` usata da tutte le rule (LAN, CREW, DRONE, ZEROTIER, default), `FAILOVER` da nessuna, **`ZT_ANY` inesistente**; zona wan `STARLINK` con masq. VM: mancavano VLAN 95/96 + tabelle `phy1-6`, rule networkd-owned e pavimenti blackhole su lan/wan/bd1 (TS-020/021/027/028), `networkd.conf.d` foreign-rules, script canonico (`126fbbc9`, zero `phy_table`), `mpquic@.service` con return-route e `mpquic-routing.timer`; binario CAL di giugno (`418d7b6`, md5 `93c8498b`), Go di sistema 1.19 (repo chiede 1.24); `global.env`/`mp1.yaml` su `172.232.209.51`; **`enp7s8` statica `10.10.99.2/29`** da `15-wan6.network.d/nqx-test.conf` e **`net13` su `vmbr0`** (residui del test NQX); istanze 1-6 a `/30` peer `.2`. Proxmox: solo `enp3s0` (mgmt) ed `enp9s0` (`vmbr7`, OpenWrt `DISH_MGMT` 192.168.100.2) con carrier; `enp8s0`/`vmbr6` (LTE) senza cavo. IBLEA-M: PHY = `eth13.96` 172.16.96.2/30 gw .1 metric 25, `eth12.95` 172.16.95.2/30 gw .1 metric 23, tracking count 3 / timeout 3 / interval 30 / down 5 / up 5 / 8.8.8.8+1.1.1.1, membri `STARLINK_PHY_M6_W1`/`LTE_PHY_M7_W1`, zona `STARLINK`; nessuna sezione `device` esplicita (VLAN per nome). Lato VM di IBLEA-M i file networkd, lo script (`bc1d0d1d`) e le unit (`b292de62`, `c1ceb385`, `dc57bffa`) sono **byte-identici ai canonici del repo**: la fonte di verità è il repo. VPS lab: binario `b25db26e` = `98f0fb9` (HEAD `ottavionovella`) con `vcs.modified=true`, `stripe_crypto_enabled: true` profilo `hybrid_security` rekey 300 s → il client deve avere la stessa base di codice e la sezione `crypto` speculare; il `mp1.yaml` di IBLEA-M (v9 TS-031: FEC off, `stripe_pacing_rate: 80`, `stripe_reseq_disable: true`, single-WAN) va bene come tuning ma non come copia letterale. CA TLS identica sui due lati.
- **Decisioni dell'utente**: build pulita di `98f0fb9` installata su client **e** VPS lab (stesso artefatto, md5 `d67f8c82`, `-trimpath` CGO off); bridge + rimozione drop-in NQX; membri PHY m6/m7 in **BONDING + FAILOVER** (ZT_ANY assente); `mp1` **dual-WAN** come il clone (wan5+wan6) con tuning IBLEA-M e sezione crypto della VPS.
- **Interventi**. VPS lab: `mpquic.bak-b25db26e-20260830`, binario `d67f8c82`, 11 istanze riavviate (mp1 in ascolto cifrato). Proxmox: `qm set 200 -net13 …,bridge=vmbr7` a caldo. VM (backup `/root/bak-ricostruzione-20260830`, 49 file + istanze): drop-in NQX via, 14/15 wan (`UseGateway=false`), 20-25 lan, 27 bd1, 28/29 phy + netdev, `networkd.conf.d`, `rt_tables` phy1-6, script canonico, `mpquic@.service` + `mpquic-routing.service/timer`, binario, `global.env` = 104.105.82.146, `mp1.yaml` nuovo, `mp1_RS_ADAPTIVE.yaml` con IP sostituito; `networkctl reload` + `reconfigure` dei soli link toccati; istanze 1-6 portate al canonico `/24` peer `.254` (commit `c14ac81`); `mpquic-vps-routes.sh` canonico `3e7e3492`. OpenWrt (backup `/root/bak-ricostruzione-20260830/{network,mwan3,firewall}`): `uci batch` con le due interfacce, tracking, membri e zona; `network reload`, `fw4 reload`, **`mwan3 restart`** (su 24.10 `mwan3 reload` non esiste e `mwan3 ifup` non crea i tracker delle interfacce nuove).
- **Tre scoperte in corsa**. (1) Nessun lease su `enp7s8` dopo il bridge: il cavo Starlink era su `vmbr3`/`enp5s0` (lease `192.168.1.33` finito su `enp7s4`/wan2, il router Starlink del banco è `192.168.1.1`); l'utente lo ha spostato su `enp9s0` → lease `192.168.1.32/24` immediato, `phy6` = `default via 192.168.1.1 dev enp7s8` sopra il pavimento, `mpq6`/`cr6`/`br6`/`df6` ripartiti da soli dal watchdog. (2) `mpq6` connesso ma riavviato ogni ~17 s ("no recent network activity", `/run/mpquic/watchdog-peer-fail`): client `10.200.6.1/30` peer `.2` contro server `10.200.6.254/24` — firma TS-020, il clone precede `c14ac81`; risolto col `/24`, peer ping 5/5 32 ms. (3) `STARLINK` (tracker dentro `mpq6`) mai online (`LOST=60`, `OFFLINE` dal boot) con percorso funzionante a ping "a mano": `tcpdump -ni any` sulla VM mostra la risposta `mpq6 In 8.8.8.8 > 10.200.6.1` de-NATtata e poi **`mpq6 Out 8.8.8.8 > 172.16.6.2`, nessun `enp7s2 Out`**; `ip route get 172.16.6.2 from 8.8.8.8 iif mpq6` → `dev mpq6`; in `main` c'era **`172.16.6.0/30 dev mpq6 scope link`** al posto della rotta connessa. Autore: `/usr/local/sbin/mpquic-vps-routes.sh` vecchio hard-coded (`ea851b31`, `ip route replace 172.16.N.0/30 dev mpqN` per tutte le istanze, pensato per il VPS) lanciato sul client dall'`ExecStartPost` della unit canonica. Il canonico (`3e7e3492`) deriva le rotte da `RETURN_SUBNETS` nell'env dell'istanza, assente sul client → no-op. Fix: script canonico + `networkctl reconfigure` dei link LAN + rimozione della rotta stantia → `route get` = `dev enp7s2`, `STARLINK` online in 60 s. Nota di metodo: `ping -I eth13` da OpenWrt **non** riproduce il tracker (con la marcatura mwan3 in OUTPUT esce da `eth8.17` dentro `mp1`); il tracker usa il wrapper `LD_PRELOAD` con device+sorgente+fwmark `0x3f00`, cioè `mwan3 use <iface> ping`.
- **Evidenze finali**: VPS: 12 pipe `mp1` registrate da `188.152.25.56`; VM: peer ping mp1 5/5 0% avg 35,5 ms, `ping -I 172.16.96.1 8.8.8.8` 5/5 0% avg 35,0 ms (20/20 0% avg 33 ms), `route get … iif enp7s2.96` → `via 192.168.1.1 dev enp7s8 table phy6`, rule 1001-1006/1017/1095/1096 `proto static`, `main` con il solo default di management, `mpq6` peer 5/5 32 ms, istanze attive 6/br6/cr6/df6/mp1, 0 restart in coda; OpenWrt: near-hop VLAN 95/96 0,2 ms, `ping -I 172.16.96.2` 20/20 0% avg 55 ms (3 s medi solo nel burst di registrazione delle pipe), `mwan3 use STARLINK` 5/5 0% 29,5 ms, `mwan3 use STARLINK_PHY` 5/5 0% 28,8 ms, `mwan3 use BOND1` 5/5 0% 33,8 ms; `mwan3 status`: **STARLINK online, BOND1 online, STARLINK_PHY online**, LTE_PHY offline veritiero (`enp8s0` carrier 0), policy `BONDING`→BOND1, `FAILOVER`→STARLINK (PHY in coda m6/m7).
- **Esito / stato**: **chiuso 🟢 sul banco**. Residui: (a) **IBLEA-M produzione**: stessa unit canonica + stesso script vecchio `ea851b31` → in `main` `172.16.3.0/30 dev mpq3` e `172.16.4.0/30 dev mpq4` al posto delle connesse (`route get 172.16.3.2 from 8.8.8.8 iif mpq3` → `dev mpq3`). Oggi innocue perché VSAT1/VSAT2 stanno sulla rete fisica dei modem (`172.30.229.2/.10`) e nessun membro usa quei /30; difetto latente per qualunque traffico che entri da `enp6s22`/`enp6s23`. Proposta: script canonico + rimozione delle due rotte, in finestra, con ok umano. (b) `wan5` in `mp1` logga "no ipv4 found on enp7s7" ogni 2 s finché l'LTE non ha cavo e lease (scelta dual-WAN). (c) default fantasma `10.10.11.1` in `main` (TS-018) lasciato: "resto invariato". (d) `mpquic-update.sh` sul clone `1749bb91` (repo `6d6cc114`) non toccato; `/opt/mpquic` del client resta a `418d7b6` (la VM non ha git verso l'esterno): il binario è tracciato per commit + md5. (e) TELESAT del banco era in realtà la LAN Starlink su `vmbr3`: ora offline, coerente.
- **Lezione → wiki**: candidate alla chiusura del residuo (a): uno script lato-server distribuito anche sul client tramite unit condivisa; il "carrier sempre su" delle virtio; `ping -I dev` su OpenWrt con mwan3 non è il tracker.

## TS-033 — Banco TBOX-EVO — STARLINK_PHY mai online dopo stacco/riattacco della NIC: bounce fisici di `enp9s0` che il tracker mwan3 (`up=5`) non perdona

- **Target**: banco TBOX-EVO — OpenWrt VM 100 (ZT `10.202.15.2`), client VM 200 `MPQUIC` (`10.10.11.100`; i DNAT `:3222`/`:2222` non rispondevano durante l'incidente, raggiunta via jump `-J root@10.202.15.2`), Proxmox `TBOX-10237840` (`10.10.11.2`). VMID verificato alla sorgente: `qm list` → VM 200 running, VM 300 `MPQ-ROMARS` stopped; `enp7s8` della VM = MAC `bc:24:11:5c:e2:da` = `net13` su `vmbr7` = fisica `enp9s0` (igc).
- **Data**: 2026-08-07
- **Sintomo**: dalle ~11 del mattino, dopo stacco/riattacco fisico della scheda di rete della TBOX EVO, `STARLINK_PHY` (`eth13.96`) non tornava più online in mwan3; anche `STARLINK` (eth13) ferma in "connecting". L'utente aveva inoltre spento la VM 300 e acceso la VM 200 (~16:19) e riavviato l'OpenWrt (~16:22), senza effetto apparente.
- **Diagnosi (read-only)**: `LTE_PHY` online → catena VLAN 95 via VM sana, quindi non era un problema di routing phy della VM. Ping manuali da OpenWrt: sorgente `172.16.96.2` → 8.8.8.8 = 20/20 a 0% loss (avg 65,6 ms, firma Starlink), near-hop `172.16.96.1` = 20/20 a 0% (0,25 ms), ARP REACHABLE. Ma `logread`/mwan3track raccontava un'altra storia: successi alternati a "Lost 4 ping(s)", score mai oltre 4 — perdita **a raffiche** che i burst brevi non intercettano (due sessioni SSH admin cadute proprio in quelle finestre, stessi minuti in cui anche TELESAT perdeva check). Sull'host Proxmox, `dmesg`: `enp9s0` Down 10:56:47 → Up 11:00:23 (lo stacco/riattacco), poi ancora due bounce da ~4 s alle 16:27:49 e 16:28:31, più `enp5s0` alle 16:31:23. Contatori L1 puliti: 0 rx/tx errors su 15,3M pacchetti, 18 carrier_changes in 4 giorni di uptime host, link 1000/Full — quindi niente CRC da cavo marcio: cadute di link secche.
- **Evidenze**: sequenza tracker 14:29:01→14:29:16 UTC: score 1,2,3,4 poi "Interface STARLINK_PHY (eth13.96) is online" — 25 s puliti sono bastati appena i bounce si sono fermati. VM 200: `enp7s8` lease `9.246.8.61/23` metric 106, `phy6` = `default via 9.246.8.1 dev enp7s8` sopra il pavimento blackhole 9999, `enp7s2.96` = `172.16.96.1/30`.
- **Causa radice**: il link fisico Starlink (`enp9s0`) ha continuato a fare bounce anche dopo il riattacco; col tracking `up=5` / interval 5 s, una perdita intermittente impedisce indefinitamente i 5 successi consecutivi → membro in connecting/offline perenne su un path che "a campione" risulta perfetto. Lo swap VM 300→VM 200 non c'entrava: entrambe hanno le VLAN phy (VM 300 dal TS-032), e la VM 200 è risultata sana a ogni verifica.
- **Esito / stato**: **chiuso, rientrato da solo** — ultimo bounce 16:28:35, `STARLINK_PHY` online 16:29:16, `STARLINK` online, stabile a +6 minuti senza nuovi flap. Non provabile a posteriori se nel pomeriggio anche il lease/route della VM 300 fosse degradato (VM spenta e OpenWrt riavviato = log persi). **Coda aperta**: `enp9s0` ha uno storico di flap notturni ~03:50 (riavvio del router Starlink, presente nei giorni precedenti — benigno); se i bounce si ripresentano in orario diurno, il primo indiziato è il connettore/porta maneggiata stamattina. Da chiarire anche perché i DNAT `:2222`/`:3222` non rispondessero ("banner exchange timeout") mentre la `:22` e il jump funzionavano.
- **Lezione → wiki**: candidate alla chiusura della coda: (1) i flap L1 del banco si leggono solo su `dmesg` dell'host Proxmox — le VM virtio hanno carrier sempre su; (2) burst di ping brevi non vedono la perdita a raffiche: prima i log del tracker, poi le misure; (3) mwan3 `up=5` + perdita intermittente = connecting perenne, non "offline": distinguere i due stati.

## TS-032 — TBOX-EVO / VM 300 ROMARS — Collegamento VLAN fisiche 95/96: chiusura A11 con variante config-only (`Gateway=_dhcp4`), software del fornitore intatto

- **Target**: VM 300 `MPQ-ROMARS` su TBOX-EVO (`10.10.11.100`, via ZT `10.202.15.2:3222`, utente satcom) + OpenWrt VM 100 (ZT `10.202.15.2`) + Proxmox `TBOX-10237840` (ZT `:2222`). VMID verificato alla sorgente prima di ogni scrittura: `qm list` → VM 200 stopped, VM 300 running; MAC di `enp7s1`/`enp7s2` sulla VM = `BC:24:11:C4:41:C2` (net6/vmbr14) e `BC:24:11:79:C9:CD` (net7/vmbr15) della config `qm config 300`.
- **Data**: 2026-08-06
- **Sintomo**: difetto A11 del `tbox/docs/PRONTUARIO_ROMARS_VM300_PHY.md` (unico rimasto aperto): lato OpenWrt le interfacce `LTE_PHY` (eth12.95, 172.16.95.2/30) e `STARLINK_PHY` (eth13.96, 172.16.96.2/30) erano pronte da luglio, ma dall'altra parte del cavo nessuno rispondeva — ARP `INCOMPLETE`, membri mwan3 offline, failover sul canale fisico non ingaggiabile. ROMARS ha autorizzato Telespazio a procedere direttamente, con due vincoli: solo collegamento delle subnet verso `enp7s7`/`enp7s8`, e nessun tocco/copia di software sulla loro VM.
- **Diagnosi**: ricognizione read-only pre-intervento identica al quadro del 3/08: `rt_tables` senza `phy5`/`phy6`, nessuna regola 1095/1096, nessun file networkd per le VLAN 95/96, script `/usr/local/sbin/mpquic-policy-routing.sh` alla versione senza blocco `phy_table` (md5 `21856433ab23acad27470ca3ae1a03e4`). Unica differenza: lease invertiti rispetto al 3/08 (`enp7s8` con 9.246.8.61/23 metrica 106, `enp7s7` momentaneamente senza IPv4, ripreso 192.168.1.x al reload).
- **Evidenze (post-intervento)**: lato VM `ping -c5 -I 172.16.96.1 8.8.8.8` = 5/5 0% avg 17,95 ms (Starlink) e `-I 172.16.95.1` = 5/5 0% avg 60,8 ms (LTE); `ip route get 8.8.8.8 from 172.16.96.2 iif enp7s2.96` → `via 9.246.8.1 dev enp7s8 table phy6` (e gemello su phy5): le regole ROMARS 999/1000 (`iif enp7s1/enp7s2`) **non** intercettano le VLAN figlie, come previsto dal §4.3 del prontuario. Da OpenWrt: near-hop 5/5 0% a 0,26-0,31 ms, ARP `REACHABLE` su entrambe; `ping -c20` sorgente `172.16.95.2` = 20/20 0% avg 60,1 ms e `172.16.96.2` = 20/20 0% avg 56,1 ms; `mwan3 status` → **`LTE_PHY` online, `STARLINK_PHY` online** (criterio di chiusura del prontuario: online, non solo configurati).
- **Causa radice**: il pacchetto VLAN fisiche non era mai stato applicato sulla baseline da cui riparte la VM 300 (vedi TS-019/TS-026 per lo stesso quadro su IBLEA-M).
- **Fix applicati (tutti e soli file di configurazione, con backup)**: snapshot Proxmox `pre-phy-vlan-20260806`; `28-vlan95.netdev`/`28-phy5.network`/`29-vlan96.netdev`/`29-phy6.network` dal riferimento versionato (`mpquic/deploy/networkd/phy/`, refuso "VLAN 95" corretto anche nel repo); `VLAN=enp7s1.95`/`VLAN=enp7s2.96` nei padre `24-lan5`/`25-lan6` (backup `.bak-phyvlan-20260806`); `rt_tables.d/mpquic-phy.conf` (`195 phy5`, `196 phy6`). **Variante rispetto al prontuario B5**: il default dinamico verso il gateway DHCP non è stato portato con il backport del blocco `phyN` nello script (avrebbe violato il vincolo "niente software"), ma con due drop-in networkd `14-wan5.network.d/phy5-default.conf` e `15-wan6.network.d/phy6-default.conf` contenenti `[Route] Gateway=_dhcp4 Table=195/196` (systemd 252): networkd installa e rinfresca da solo il default nella tabella phy a ogni lease, lo rimuove a lease perso lasciando il pavimento blackhole, ed è immune per costruzione allo sweep A9. Nota per il futuro: se su questa VM si applicherà mai lo script canonico col blocco `phyN`, i due drop-in vanno rimossi (doppio gestore dello stesso default).
- **Esito / stato**: **chiuso 🟢**. Software ROMARS e script di policy routing intatti (md5 `21856433` riconfermato a fine intervento). Entry HANDOFF "fornitore ROMARS" del 2026-08-03 aggiornata a risolta.
- **Lezione → wiki**: variante config-only del popolamento tabelle phy (`Gateway=_dhcp4` in drop-in) per macchine dove lo script non si può toccare — distillata in `wiki/`.

## TS-031 — Banco TBOX-EVO — Upload STRIPES al collasso: TX mai pacato, alternanza path per-pacchetto, e il pacing che esisteva ma non era mai stato acceso

- **Target**: trasporto STRIPES mp1 — client VM 200 lab (`10.10.11.100`, via ZT `10.202.15.2:3222`) + VPS lab (`104.105.82.146`). VMID verificato su Proxmox (`qm list`: VM 200 running, VM 300 ROMARS stopped).
- **Data**: 2026-07-29 (loop su mandato utente: "perfeziona STRIPES finché l'upload non è equiparabile al fisico, senza peggiorare il download")
- **Sintomo**: upload dentro mp1 disastroso su entrambi gli impianti — IBLEAM: 22.7 Mbit che decadono verso zero (2180 retx) contro 342 Mbit del fisico; banco EVO: 25.5 Mbit (5975 retx) contro 53-103 del fisico. Download -P30 sano ovunque (IBLEAM 323, banco 150-270).
- **Diagnosi (strumentata, contatori `/api/v1/stats` su entrambi i lati)**: nel run di baseline il trasporto ha consegnato **tutto** (client TX 33.150 pkt, server RX 33.152; NACK ARQ ~0; `adaptive_m=0`; `loss_rate=0`) eppure iperf contava 5975 ritrasmissioni TCP: **tutte spurie, da riordino**, non da perdita. Tre concause distinte:
  1. **TX client mai pacato**: `stripe_pacing_rate` assente da ogni config → né pacer software né SO_TXTIME; i batch GSO (fino a 64KB) partono a line-rate virtio dentro un uplink Starlink da ~50 Mbit → burst loss e cwnd interna al collasso.
  2. **Alternanza di path per-pacchetto**: in `selectBestPath`, a parità di punteggio, `m.rr` ruota a ogni pacchetto tra wan5 (RTT 67 ms) e wan6 (29 ms): ogni flusso subisce riordino cross-path sistematico. (Il server ha il dispatch per-flow dal commit `4ae19a9`; il client no.)
  3. **Il pacing kernel era comunque inerte**: le NIC avevano `fq_codel`, non `sch_fq` — il prerequisito è scritto nel commento di `stripe_txtime_linux.go` ("tc qdisc replace dev <wan> root fq") ma non è mai stato applicato al banco: SO_TXTIME/EDT e SO_MAX_PACING_RATE erano ignorati dal kernel.
- **Evidenze chiave**: tetto fisico misurato nello stesso minuto di ogni A/B (lezione TS-016): wan6 38-76 Mbit, wan5 11-19 a seconda dell'ora; test E (pacing 50, single path): 46.8 su 53.4 di tetto = 87%; test F (affinity + pacing per-pipe): 8.25 Mbit = trappola del cap rate/12 per pipe; A/B interallacciato v4+fq vs originale: 51.7 (680 retx) e 8.6 vs 27.2/27.7 (5.3-5.6k retx) — il rate statico sopra la capacità istantanea ricrea i burst persi; con AIMD: 69.7 e 50.9 contro 51+15 di tetto combinato. Affinity client non gatata: P1 download crollato a 387 Kbit (ACK del flusso pinnati su pipe CGNAT marcia = ripetizione esatta di TS-024).
- **Causa radice**: il trasporto in salita non ha mai avuto né pacing né ordinamento per-flusso: round-robin per-pacchetto su 12-24 pipe consegnato al TCP interno come riordino massiccio, più burst GSO non pacati sull'uplink stretto. Il fix v5.1 (flow-affinity) aveva curato solo la direzione download (lato server).
- **Fix applicati** (branch `feat/ts031-upload-pacing` da `3a31bf9`, 7 commit, binario finale `57608187` deployato sul solo banco):
  - `pacing_rate` per-path in `MultipathPathConfig` (uplink asimmetrici: cap 25 wan5 / 80 wan6);
  - `multipath_flow_sticky` (opt-in, default off): tie-break per hash FNV del 5-tuple interno sui candidati a **score strutturale** minimo (priority/weight, senza `failPenalty`: il primo taglio migrava in massa i flussi a ogni errore transiente — 86% degli ACK su un solo path); su errore di send il retry ricade sulla selezione classica;
  - EDT del pacing **proporzionale ai byte** (`gap×len/1402`): l'avanzamento per-pacchetto spaziava gli ACK da 90B come shard da 1402B e cappava il ritorno ACK in pps (download 271→128, regressione trovata e corretta in A/B same-minute);
  - `dynamicPacingLoop` riscritto: lo stub ricalcolava il gap come `1e9/rate` (20 ms!) ogni 200 ms — mai visto in esercizio solo perché l'EDT era ignorato; ora **AIMD** guidato dai NACK del server (`arqRetxSent`): loss >2% → rate ×0.7, loss <0.5% → +1 Mbit/tick, tra 2 Mbit e il cap;
  - EDT **condiviso per path** (non più per-pipe a rate/12): il budget di pacing è dell'uplink; SO_MAX_PACING_RATE a rate pieno per socket come backstop dei pacchetti non stampati;
  - **flow-affinity client con health-gating per-pipe**: il server risponde a ogni keepalive sulla stessa pipe (1 Hz) → `pipeHealthyMask` ricalcolata a ogni tick (RX entro 3 intervalli), `dataPipeIdx` hasha solo sulle pipe sane, maschera vuota = degradazione a tutte-candidabili. Chiude per costruzione il veto TS-024 all'affinity client. Unit test: mask rispettata, scelta deterministica per flusso, mai blackout di TX;
  - `sch_fq` sulle WAN del banco, persistito con `net.core.default_qdisc=fq` (sysctl).
- **Esito / stato**: **chiuso sul banco**. Upload P30 41-70 Mbit con tetto fisico 38-76 misurato nello stesso minuto (baseline 22-29 con 5-6k retx; retx ora 611-1583); download invariato nei run puliti (233-271 vs 271 dell'originale same-minute); P1 download 36-48. Residuo P1 upload 0-16 Mbit a lotteria: stalli improvvisi di wan5 (uplink nero per secondi con eco che sparisce — famiglia TS-015, substrato non algoritmo); mitigazione futura = migrazione mid-flow su stallo. **Config e binario NON deployati in produzione** (decisione gated: IBLEAM ha la stessa malattia e il fix è candidato, ma serve finestra e ok umano; su IBLEAM servirebbe anche `fq` sulle WAN e i cap AIMD tarati sul suo uplink). Consumo dati Starlink della campagna: ~3-4 GB.
- **Trappole di misura documentate per i prossimi**: (1) la capacità Starlink del banco oscilla ±3x in minuti: ogni A/B senza tetto fisico nello stesso minuto e senza interallacciamento mente; (2) iperf3 dentro il tunnel si impianta spesso al teardown ("unable to receive results", run che restano appesi 20-40s e falsano le medie): sempre `timeout` wrapper e restart del server iperf3 tra i run; (3) `pkill -f "iperf3 -c"` via ssh uccide anche la propria shell (il pattern matcha il wrapper bash); (4) porta 5202 chiusa dal firewall VPS: usare la 5201; (5) `cp` su un binario in esecuzione fallisce con "Text file busy" e il restart rilancia il binario NUOVO: per il rollback usare `install` (unlink+create).
- **Lezione → wiki**: distillata in `wiki/mpquic/` (pacing e ordinamento per-flusso su trasporti multi-pipe; vedi anche l'aggiornamento della lezione TS-024).

### ⚠️ ADDENDUM (stesso giorno, pomeriggio) — il "chiuso" sopra era un FALSO SUCCESSO; riaperto dall'utente e richiuso con causa vera

- **Cosa è successo**: l'utente ha rifatto i test col SUO metodo (da OpenWrt, `mwan3 use BOND1 iperf3 -c 104.105.82.146`, 10s, run back-to-back) e ha trovato il **download regredito al disastro** (165→124 poi 104→67, e 20→0 in upload a fine run). La validazione che aveva "chiuso" il caso era metodologicamente sbagliata: dal client VM verso l'IP di tunnel, run corti, mai back-to-back, senza replicare il percorso reale (router → forwarding → IP pubblico). **Regola dura: il collaudo si fa col metodo e dal punto di osservazione dell'utente, non con quello comodo per chi sviluppa.**
- **Review del transport-expert** (su richiesta utente, "fai intervenire il team") — tre scoperte:
  1. **Debito EDT illimitato** (causa primaria vera): con offerta sopra il rate, `txtimeNextEDT` correva nel futuro senza limite → coda `sch_fq` satura (`flow_limit 100p` per pipe) → **drop silenziosi nel qdisc locale** (misurati 479 dropped e 95 horizon_drops su wan5: pacchetti stampati oltre 10s nel futuro) → perdita auto-inflitta che qualunque segnale di loss scambia per congestione → anello di retroazione positiva. Fix: **clamp dell'orizzonte a 15ms** ("il pacer è uno smussatore di burst, non un controllo d'ammissione"), invariante [I1].
  2. Il segnale `peerLossRate` per l'AIMD è **rotto lato server**: `computeSessionRxLoss` consuma i delta e i 12 keepalive arrivano in burst → 11 risposte su 12 valgono 0. AIMD messo **dietro flag `stripe_pacing_adaptive` (default off)**: rate statico al cap finché il fix server (C2) non c'è.
  3. L'esenzione dei "pacchetti piccoli" dal pacing andava fatta sul **pacchetto interno (solo ACK puri**, niente payload/SYN/FIN/RST), non sulla taglia wire (un segmento piccolo CON payload che scavalca genera fast-retransmit spuri), e i byte esenti vanno **comunque addebitati** al budget EDT ([I2]/[I3]). Aggiunto anche il dedup delle retx ARQ (stesso seq max ogni 100ms: il server ri-NACKa ogni 30ms con RTT 40-70ms → 2-3 copie per buco, misurati 1411 duplicati).
- **La causa del decadimento del download** (quella che l'ottimizzazione aveva slatentizzato ma che era preesistente): **la FEC adattiva è un cricchetto tossico su downlink bursty**. Prova A/B/C back-to-back: fisico diretto 385/255/402 (nessun degrado run-su-run) vs tunnel FEC adaptive 256→132→79 (decadimento monotono) vs tunnel **FEC off 246/235/245/277 (stabile)**. Meccanismo: burst di loss → `adaptive_m` sale → 20-30% parità+padding sul downlink già in affanno → più loss → più FEC; in idle M si rilassa → "primo run pulito, i successivi malati" (la firma che aveva ingannato tutta la giornata di A/B). Conferma della lezione TS-013 ("FEC alta peggiora il link congestionato"), mai applicata alla modalità adaptive.
- **Stato finale del banco (v9, commit `6429992`, binario `60965c62`)**: pacing caps statici 25/80 con clamp orizzonte, sticky+affinity health-gated, `sch_fq` persistito, **`stripe_fec_mode: off` su mp1 dei due lati**. Misure col metodo dell'utente: download back-to-back 235-277 (soak 30s: **232 medi**, min intervallo 163 con recupero pieno), upload back-to-back 60-71 (soak 30s: **65 medi**, min intervallo 48.2, **zero sessioni a 0**), contro target 200/50. Ri-verifica a distanza di tempo prima della chiusura definitiva.
- **Aperti per il prossimo giro (dalla review)**: C2 (fix `computeSessionRxLoss` idempotente in finestra, server-side, prerequisito per riattivare l'AIMD), esclusione dei seq NACKati dalle statistiche maxOOO del tracker (ogni recupero ARQ oggi rallenta l'ARQ successivo), retx sulla pipe originale, dedup retx a `max(100ms, 1.5×srtt)`, metriche mancanti (`edt_debt_ms`, `pacing_rate_mbit`, `paced_exempt_bytes`, `retx_suppressed`), e la domanda di prodotto: la FEC adaptive va ripensata (soglie/isteresi/cap di M) o sostituita da ARQ-only sui profili bursty.

### 🚀 DEPLOY IN PRODUZIONE IBLEA-M (stesso giorno, ~16:00, su mandato utente) — tag `v5.2`

- **Rilascio**: branch e tag **`v5.2`** pushati su GitHub (`feat/ts031-upload-pacing`, 9 commit da `3a31bf9`). Binario `60965c62` deployato via scp su **VPS IBLEA** (identità verificata: 172.238.232.223, stesso hostname del lab — controllare sempre l'IP) e **client VM200** (identità verificata; accesso via jump nel tunnel → deploy protetto da **dead-man a 300s** armato prima del restart e disarmato a verifica ok). Backup su entrambi: `.bak-ts031-v52` (binario + mp1.yaml). Riavviata la SOLA istanza mp1 per lato; le altre 10+ istanze restano sul binario precedente in memoria (compatibile, wire invariato).
- **Config produzione mp1**: `stripe_fec_mode: off` su ENTRAMBI i lati (il cricchetto FEC era la causa del decadimento anche qui), `stripe_pacing_rate: 80` sul client (tetto fisico misurato 65-86 a seconda della finestra; il clamp gestisce l'overshoot), `sch_fq` su `enp7s8` + `net.core.default_qdisc=fq` persistito. Affinity/sticky NON attivate (single-path; conservativo, coerente col verdetto A/B di TS-027 sul substrato di bordo).
- **Validazione dal TBOX-IBLEAM col metodo del cliente** (`mwan3 use BOND1 iperf3 -c 172.238.232.223`, tetto fisico via `STARLINK_PHY` nello stesso minuto): upload **42.9 e 43.5 Mbit back-to-back** (ogni intervallo 40-58, zero stalli) contro 65.4 di fisico = **66% del tetto** — al mattino era 22.7 Mbit che decadeva a ~1 con 2180 retx; download **145 → 163 back-to-back** (in salita, niente cricchetto) contro 277 di fisico = 59%. In proporzione alla finestra del mattino (fisico up 86) l'upload atteso è ~55-60.
- **Stato**: **CHIUSO, in produzione.** Residui di tuning (non bloccanti): efficienza download 59% su IBLEAM (substrato di bordo + riordino RR residuo), P1 single-flow, riattivazione AIMD dopo C2.

## TS-028 — IBLEA-M produzione — Reboot di collaudo e fine del bypass implicito: gateway dai lease, default fisici fuori da main

- **Target**: client VM200 IBLEA-M (`satcom@10.200.17.1` via VPS, hostname `mpquic`). Via d'emergenza indipendente dai tunnel ri-verificata prima di iniziare: `ssh -p 2222 root@10.202.11.2` (Proxmox `TBOX-10241434` via ZeroTier) + `qm status 200`.
- **Data**: 2026-07-29 mattina (loop autonomo, su mandato esplicito dell'umano: reboot al posto del restart networkd + completare i task residui + chiudere il loop a fine lavoro)
- **Fase 1 — Pre-flight**: assimilato TS-027 (stato cambiato nella notte: affinity spenta, rule networkd-dichiarate, binario `63851a95`). Scoperto e sanato un **drift di boot**: le quattro istanze della famiglia `*4` (`mpquic@4`, `br4`, `cr4`, `df4`) erano attive ma `disabled` — un reboot le avrebbe perse in silenzio. Abilitate prima del riavvio.
- **Fase 2 — Reboot (collaudo di persistenza)**: eseguito staccato via `systemd-run`. VM su in ~1 minuto: **13/13 istanze ripartite da sole, 0 failed**; rule 1001-1006/1017/1095/1096 tutte **`proto static`** sopravvissute al boot (TS-021 chiusa anche a riavvio); tabelle phy popolate; wan6 fail-closed; timer e watchdog attivi; **drop-in `ManageForeignRoutingPolicyRules=no` finalmente attivo** (era il residuo dichiarato di TS-027); LTE tornato in servizio (VLAN 95 → 0% loss, ~130-180 ms).
- **Fase 3 — Parte (b), in due tempi per colpa di un gotcha**:
  - `gw_for_dev` esteso con fallback sui lease di systemd-networkd (`/run/systemd/netif/leases/<ifindex>`, chiave `ROUTER=`, verificata presente prima di scrivere codice): la detection non dipende più dal default in `main`.
  - `UseGateway=false` su `14-wan5`/`15-wan6` + rimozione dei default via `192.168.11.1`/`192.168.1.1`. **Primo tentativo inefficace in silenzio**: la chiave era nella sezione legacy `[DHCP]`, dove systemd 252 la ignora con `Unknown key ... ignoring` — il renew-test l'ha smascherato (default ricomparso). Spostata in `[DHCPv4]`: renew forzato su entrambi i fisici → il default **non torna**, `phy5`/`phy6` restano popolate dal lease. Ogni applicazione protetta da dead-man (`systemd-run --on-active=420` con ripristino di script+file+reload), mai scattato.
- **Fase 4 — Coda**: dopo i renew forzati, mp1 a **50-70% di loss deterministico sul peer ping con journal muto e SSH nel tunnel funzionante**, fisico a 0% nello stesso minuto (via VLAN 96): firma TS-015 di N pipe su 12 con binding CGNAT morto dal renew, che il TX usa comunque. Bounce di `mpquic@mp1` staccato → **20/20, 0% loss, ~42 ms**. Caveat operativo nuovo: **un renew/rotazione DHCP sulla WAN di un'istanza stripe può richiedere il bounce dell'istanza**; il candidato di trasporto resta l'esclusione dal dispatch delle pipe non confermate end-to-end.
- **Effetto collaterale dichiarato**: senza default fisici in `main`, la diagnostica per-device cambia — `ping -I enp7s8 8.8.8.8` nudo ora fallisce per costruzione (nessuna route); il pavimento fisico si misura dalla sorgente VLAN (`ping -I 172.16.96.1`), che è anche il percorso reale del failover. Aggiornare le abitudini di misura di conseguenza.
- **Esito / stato**: **chiuso 🟢**. Il bypass implicito TS-017 non esiste più in nessuna forma: il gateway fisico vive solo nelle tabelle phyN, il fallback fisico è solo la decisione esplicita di mwan3. Repo: commit `0a19b60`. Backup sul campo: `.bak-pre-ts028`, `.bak-ts028` (wan5/wan6), snapshot `pre-v51-20260728`. Su mandato umano, con questa chiusura il loop autonomo mpquic si ferma e il controllo passa al team tbox (registro aggiornato).
- **Lezione → wiki**: (1) gotcha `[DHCPv4]` vs `[DHCP]` legacy: le chiavi nuove nella sezione vecchia muoiono in silenzio, e solo un test funzionale (renew) lo rivela; (2) rinforzo TS-015: anche un semplice renew DHCP può ammazzare i binding CGNAT delle pipe senza che la liveness se ne accorga — bounce dell'istanza dopo operazioni DHCP forzate sulla sua WAN.

## TS-027 — IBLEA-M produzione — Chiusura dei residui: addressing /24, la malattia TS-021 ancora viva, e il v5.1 che non era mai entrato in esercizio

- **Target**: client VM200 IBLEA-M (`satcom@10.200.17.1` via `-J vps-mpquic-it-tpz-iblea`, hostname `mpquic` verificato) e VPS `172.238.232.223`. Via d'accesso indipendente dal tunnel verificata prima di toccare qualsiasi cosa: `ssh -p 2222 root@10.202.11.2` (Proxmox `TBOX-10241434` via ZeroTier) con `qm guest exec 200` funzionante.
- **Data**: 2026-07-28 notte / 2026-07-29
- **Mandato**: chiudere i residui di TS-025 (addressing `/30`→`/24`, anomalia mpq5, script canonico) e riportare il deploy sulla via dello script, su client e su server.
- **Diagnosi (read-only prima di ogni intervento)**:
  - Addressing: istanze 1-4 ancora `10.200.N.1/30` peer `.2`; 5, 6 e mp1 già `/24` peer `.254`. La forma `/24` era validata sul banco da TS-020 ma nel repo i template erano rimasti tutti `/30`, quindi la migrazione sul campo non aveva una sorgente da cui ripartire.
  - `mpq4`: 60-81% di loss ICMP nel tunnel contro un fisico `enp7s6` allo 0-33% nello stesso minuto, con `ERROR reconnect in 3s: INTERNAL_ERROR (local): write udp 172.30.229.2:...->172.238.232.223:45004: sendmsg: operation not permitted` ogni pochi secondi. Campionando `ip rule` ogni 1.5s: la rule 1204 assente in 18 campioni su 30 (finestre da ~27s), con la route host `/32` sempre presente. Correlazione trovata nel journal di networkd: `enp7s5: Configuring with /etc/systemd/network/12-wan3.network` seguito da `DHCP lease lost` e riacquisizione, a intervalli di pochi minuti sui due GEO. Ogni riconfigurazione di link spazza le rule foreign, comprese quelle di *altre* WAN.
  - Confronto con il template di laboratorio (VM 200 lab, la macchina da cui si clonano le navi): sul banco le rule 1001-1006 sono `proto static` perché dichiarate nei file networkd delle LAN, c'è `ManageForeignRoutingPolicyRules=no` in `networkd.conf`, il timer `mpquic-routing.timer` è attivo e `mpquic-if-event.sh` è la versione corrente. Su IBLEA nessuna delle quattro cose.
  - Binario: `/usr/local/bin/mpquic` (quello che le unit eseguono davvero, verificato via `/proc/PID/exe`) era `cb5eb0d0` sul client e `4ac2bed3` sul VPS. Il v5.1 di TS-025 stava in `/opt/mpquic/bin/mpquic` e non era mai stato installato: **il miglioramento misurato quella sera veniva dalla bonifica del routing, non dal binario nuovo**, e la flow-affinity lato server non era mai stata realmente in funzione (il binario CAL ignora la chiave).
- **Causa radice**: due difetti di propagazione, non di codice. Il primo è TS-021 lasciata a metà in produzione: le `ip rule` scritte a mano sono "foreign" per networkd, che le rimuove a ogni riconfigurazione di link, e senza timer di riconciliazione nessuno le rimetteva. Il secondo è che il ramo di produzione (`feat/flow-affinity-3c03829`) portava il codice v5.1 ma gli script e le unit di deploy fermi a prima di TS-021/TS-026: siccome `mpquic-update.sh` installa `mpquic@.service` e gli script di libreria *dal repo*, il primo update ha rimesso una unit senza `ExecStartPost` (le return route di TS-012) e si è fermato a metà elenco su `mpquic@6` mascherata, perché `set -e` fa morire lo script su uno start fallito.
- **Fix applicate**:
  1. Addressing `/24` peer `.254` su istanze 1-4, prima nei template del repo poi sul campo con backup `.bak-addr24-20260728`, riavviando le coppie server→client una alla volta. Le istanze 1 e 2 restano ferme (WAN assenti) ma con la config pronta.
  2. Wan rule 1001-1006 dichiarate nei file networkd delle LAN (`[RoutingPolicyRule]` + pavimento `blackhole` per tabella), come sul template di laboratorio: ora sono `proto static` e lo sweep non le tocca.
  3. `mpquic-routing.timer` installato e attivo (non esisteva su IBLEA) e `mpquic-routing.service` con `RemainAfterExit=no`, altrimenti il timer non riesegue mai.
  4. `mpquic-if-event.sh` portato alla versione corrente (`1c08e19a`): dopo ogni evento di link il routing si ricostruisce in un secondo invece di aspettare il timer.
  5. `mpquic-update.sh`: le unit mascherate escono dalla lista e uno start fallito non fa più morire l'update a metà.
  6. Ramo di produzione riallineato (`git checkout ottavionovella -- deploy/ scripts/`, commit `3a31bf9`) e deploy rifatto **con lo script** su VPS e client. Sul client il `git fetch` dalla nave resta impraticabile, quindi il commit viaggia come **bundle git** da 15 KB via scp e poi `git merge --ff-only`: il percorso resta quello standard, cambia solo come arrivano gli oggetti.
- **Evidenze numeriche (dopo)**: rule 1203-1206 presenti in 30 campioni su 30; `mpq3`, `mpq4`, `mpq5` a **0% loss** (erano 0%, 60-81%, 100% sulla vecchia forma); mp1 al 12% quando il fisico Starlink stava al 12% nello stesso minuto, con RTT più stabile del fisico (max 57 ms contro 645); md5 del binario **identico sui due host** (`63851a95`, commit `3a31bf9`); update completo su 15 istanze client e 11 VPS.
- **A/B della flow-affinity (la scoperta che conta)**: con la flag finalmente attiva davvero, il download single-stream dentro mp1 stava a **0.3-0.7 Mbit** mentre il fisico Starlink, misurato fuori tunnel nello stesso minuto, dava **73.5 Mbit** con 0% di loss ICMP. Spenta la flag sul VPS (`stripe_flow_affinity: false`, backup `.bak-affinity-on-20260729`) e riavviato mp1: **21 e 39 Mbit** su due campioni, upload 61.5, aggregato P10 **249 Mbit**. Su questo link l'affinity fa danno, all'opposto del banco: il flusso resta inchiodato su una pipe sola e sul CGNAT di bordo quella pipe non vale 1/12 della capacità. La flag resta nel codice, default spenta, e va rivista come scelta per-installazione.
- **Esito / stato**: **chiuso**. Aperto e volutamente non fatto: il drop-in `/etc/systemd/networkd.conf.d/10-mpquic-foreign-rules.conf` con `ManageForeignRoutingPolicyRules=no` è installato ma si attiva solo al riavvio di `systemd-networkd`, che sulle WAN con `KeepConfiguration=no` significa rilasciare e riprendere i lease. Va fatto in finestra concordata: finché non si fa, il buco dopo ogni sweep resta, ma è coperto dal timer da 30s e dall'hook di evento.
- **Lezione → wiki**: candidate — (1) il ramo che sta in produzione deve portare gli script operativi, non solo il codice: un deploy script che installa unit e script dal repo riporta indietro l'operatività se il ramo è vecchio; (2) verificare sempre `/proc/PID/exe`, non il binario che si è copiato: il file in `/opt/.../bin` non è quello che gira; (3) `go build` timbra la revisione VCS nel binario, quindi l'md5 identico fra host vale solo a parità di commit e di worktree pulito; (4) una unit mascherata è una scelta dell'operatore e un deploy non deve morirci sopra; (5) la flow-affinity è dipendente dal substrato: va validata per installazione, non promossa a default.

## TS-026 — IBLEA-M produzione — Tabelle phy5/phy6 solo blackhole: il deploy v5.1 aveva portato le VLAN ma non lo script che le popola

- **Target**: client VM200 IBLEA-M (`satcom@10.200.17.1` via VPS), hostname `mpquic` verificato prima di operare.
- **Data**: 2026-07-28 sera (loop autonomo, handoff tbox nel registro `/opt/TPZ/src/HANDOFF.md`, loro TS-019)
- **Sintomo**: tbox ha replicato le VLAN `.95/.96` su OpenWrt IBLEA ma i membri mwan3 `STARLINK_PHY`/`LTE_PHY` restano offline: il traffico `.96` esce a ~600 ms con 75% loss (i numeri del default di VM200 su `enp7s5/enp7s6`) mentre il dish, interrogato dal client, risponde a 25 ms con 0% loss.
- **Diagnosi (read-only)**: su VM200 `ip route show table 196` = solo `blackhole default metric 9999` + `/30` connesso; idem la 195. Le rule `1095/1096` e le VLAN `enp7s1.95`/`enp7s2.96` ci sono (deploy networkd ok). Il commento nei file networkd del repo dice esplicitamente che il default dinamico lo mantiene `mpquic-policy-routing.sh` — ma lo script deployato è datato 23/07 (md5 `99229f65`, 8091 byte) e **non contiene la logica `phy_table`** introdotta col pacchetto TS-023 sul banco. Il gateway era rilevabile (main: `default via 192.168.1.1 dev enp7s8 proto dhcp metric 106`): mancava solo chi lo copiasse nella tabella.
- **Causa radice**: il deploy v5.1 (TS-025) ha applicato i file networkd delle VLAN phy ma non ha aggiornato `mpquic-policy-routing.sh`: metà del meccanismo. Le tabelle sono nate col solo pavimento blackhole dichiarato da networkd.
- **Fix applicata (reversibile)**: backport chirurgico del **solo blocco phyN** (9 righe, `ip route replace default via $gw dev $dev table $((191+idx))` se `wan_usable`, altrimenti blackhole) dal canonico `scripts/mpquic-policy-routing.sh` del repo dentro lo script deployato. NON deployato il canonico intero, di proposito: il canonico ha rimosso il bypass TS-017 (`BYPASS_WANS=enp7s8` → default fisico in tabella wan6/105), e il vincolo di sequenza dell'handoff impone di toglierlo solo in sincrono con tbox, a membri mwan3 verificati online. Backup: `/usr/local/sbin/mpquic-policy-routing.sh.bak-ts019-20260728`. Applicato con `install` atomico + `systemctl restart mpquic-routing.service`; lo script gira anche dai hook networkd-dispatcher (`routable.d`/`configured.d`), quindi il default segue le rotazioni DHCP.
- **Evidenze post-fix**: `phy6` = `default via 192.168.1.1 dev enp7s8` sopra il pavimento 9999; `ip route get 9.9.9.9 from 172.16.96.2 iif enp7s2.96` → `via 192.168.1.1 dev enp7s8 table phy6`; ping vivo sorgente `172.16.96.1` → **5/5, 0% loss, avg 34.8 ms** (masquerade per-oif confermato). `phy5` = `default via 192.168.11.1 dev enp7s7`, route get corretto, ma ping 0/5: **l'uplink LTE è morto a monte** (gw `192.168.11.1` REACHABLE a 0.58 ms, carrier 1, eppure 100% loss anche con `ping -I enp7s7` nudo, fuori dal percorso VLAN). Non è il routing: è lo stesso quadro di TS-015 (lease+carrier ma upstream nero); mwan3 che lo vede offline è lo stato veritiero. Tabella wan6/105 col bypass: intatta. Sessione di controllo (mp1) mai persa durante l'intervento.
- **Esito / stato**: chiuso lato mpquic, turno passato a tbox nel registro handoff per la verifica `STARLINK_PHY` online in mwan3 (l'`LTE_PHY` resterà offline finché l'uplink LTE non torna). La rimozione del bypass wan6 resta il passo 3, in finestra sincronizzata, e comporterà il deploy dello script canonico completo (chiude anche il drift interim di questo backport). Rollback: ripristino del `.bak-ts019-20260728` + restart della unit.
- **Verifica tbox (stessa sera)**: `STARLINK_PHY is online` in mwan3 su OpenWrt (stabile su due check a 40s); path `.96` da OpenWrt = 0% loss, 29-42 ms (era ~600 ms / 75%); ZeroTier intatto durante la verifica. Il failover fisico Starlink è operativo come ultima risorsa in FAILOVER e ZT_CLEAN. Il passo 3 (script canonico + rimozione bypass wan6 + decisione sul default DHCP in `main`, che oggi è anche la sorgente della gateway-detection dello script) è marcato 🧑 nel registro: parte solo in finestra autorizzata dall'umano, entrambi i team pronti.
- **Coda post-passo-3 (stessa sera): degrado accesso admin ZeroTier, rimbalzato da tbox.** Lockstep tbox: core del passo 3 OK (phy6 vivo, STARLINK_PHY online, mwan3 veritiero) ma ZT admin verso OpenWrt a 37% loss / RTT 75-100 ms subito dopo la finestra, SSH morta al banner. Diagnosi mpquic: (1) return `172.16.17.2` INTEGRO (route VPS ok, `route get iif mp1` ok, ARP REACHABLE; i "Destination Port Unreachable" del ping VPS→OpenWrt sono il REJECT del firewall wan di OpenWrt, e il loro ritorno 30/30 prova il giro completo mp1 a 0%); il canonico non tocca né bd1 né il VPS. (2) ARQ client wan6: 13.5k NACK / 13k dup accumulati nella sessione = episodi reali di loss downstream → ipotesi pipe con binding CGNAT degradato + flow-affinity server che ci inchioda i flussi (trade-off noto TS-024). Azione: **bounce di `mpquic@mp1` lato client** via systemd-run staccato (la nostra stessa SSH viaggia in mp1), giù ~40 s, watchdog ok, 0 restart a cascata; post-bounce ARQ fermo a 0 NACK dopo minuti = downstream pulito. **Erratum di misura (candidato wiki alla chiusura)**: ping a raffica <1s contro un REJECT mentono — il kernel rate-limita gli ICMP error a ~1/s: a 0.2s si "misura" 60-70% di loss su un path perfetto (primo verdetto "63% sulla pipe" ritirato per questo). Misurare a ≥1s o con flussi dati reali. **Rimisura tbox col metodo corretto: ZT 20/20 @1s a 0% loss, SSH fluida in ~1 s → caso chiuso 🟢** (il 37% iniziale era loss downstream reale pre-bounce + artefatto di misura anche lato tbox, `ping -i 0.5`). Latente documentato nel registro: se il loss ZT si ripresenta, il candidato è la flow-affinity server (rollback funzionalità v5.1 = decisione umana). Lezioni distillate in wiki: trappola rate-limit ICMP in `wiki/tbox/troubleshooting-lessons-flotta.md`, firma/cura della pipe marcia sotto affinity in `wiki/mpquic/deploy-infrastruttura-operazioni.md` (raw: `raw/mpquic/2026-07-28-ts026-passo3-bounce-icmp-ratelimit.md`).
- **PASSO 3 eseguito (stessa sera, finestra autorizzata dall'umano, ~4 min)**. Ricognizione pre-finestra con due scoperte: (1) le rule 1001-1006/1101-1106/1203-1206 erano state SPAZZATE da networkd (malattia TS-021: in produzione le rule wanN non sono networkd-dichiarate) → il /30 delle WAN cadeva in main e usciva su `enp7s5` (il link a 600 ms/75%); le tabelle wan1/wan2 non avevano NEMMENO il pavimento blackhole; (2) sulla VM girano anche le istanze per classe di traffico `br/cr/df` 4-6 (tabelle `mt_*` 120-128, bind per-device, solo VPS via host route): il canonico non le tocca. Esecuzione: backup `.bak-ts026-step3-20260728` del backport, dead-man `systemd-run --on-active=300` che ripristina backport+restart in caso di perdita accesso, install canonico (md5 `728e05ae` = repo), restart `mpquic-routing`. Esito: tabella 105 `blackhole default` + host route VPS conservata (bypass wan6 RIMOSSO), pavimenti ovunque, rule 100x/120x ri-aggiunte, `phy5`/`phy6` intatte. Verifiche entro la finestra: VLAN 96 → 4/4 0% loss ~37 ms; mp1 peer 20/20 0% loss ~43 ms; 0 restart istanze; `main` NON toccato (decisione condivisa: è la sorgente della gateway-detection, il sostituto va progettato prima). Dead-man disarmato a verifiche chiuse. Nota: LTE di nuovo nero a monte (100% loss sul link nudo, lease invariato) — flapping, non effetto della finestra. **Residuo gated**: deploy dei file networkd wan (rule `proto static`) per immunizzare le rule dallo sweep; tocca i link WAN, serve finestra dedicata. In attesa della verifica lockstep tbox per chiudere il caso.
- **Lezione → wiki**: caso puntuale di deploy parziale, la lezione generale ("un meccanismo diviso tra file dichiarativi e script va deployato intero, o la metà mancante lascia lo stato di default") è contigua a quelle già distillate sul drift config: non aperta una pagina nuova.

## TS-024 — Banco TBOX-EVO — Flow-affinity per pipe: il fix notturno del single-stream, e perche' la forma giusta e' asimmetrica

- **Target**: trasporto STRIPES, tunnel mp1 (client VM 200 + VPS lab), baseline pre-CAL `3c03829`. Notte 2026-07-27/28, su mandato "studia una strategia ottimale, deploya e testa".
- **Strategia scelta**: tra reseq riprogettato e flow-affinity, scelta l'affinity come fix a rischio minimo implementabile e validabile in una notte: cambia SOLO la selezione della pipe in TX (hash FNV-1a del 5-tuple del pacchetto interno invece del round-robin), zero modifiche al wire, dietro flag `stripe_flow_affinity` default off. Parity, repair XOR/RLC/RSIL, keepalive e retx restano round-robin (il loro ordine non interessa a TCP). Frammenti non-primi hashati sul 3-tuple. Fallback automatico al RR per pacchetti non parsabili.
- **Implementazione**: branch `feat/flow-affinity-3c03829` (commit `4c469dc`), helper `innerFlowHash` in `cmd/mpquic/stripe_affinity.go`, metodo `dataPipeIdx` su client e sessione server, 4 siti DATA patchati (fast-path + loop shard FEC, entrambi i lati). Build `72ad66c4`, unit test area stripe verdi, deploy su client e VPS lab con backup `.bak-pre-affinity`.
- **A/B (pclab, inoltrato, linea pulita ~03:00)**: con affinity su entrambi i lati: P1 -R **85.2 Mbit** (da 0.3-21) e P30 -R 187 Mbit, ma **upload crollato a 104 Kbit** (da 17-47). Causa del crollo: un flusso inchiodato a UNA pipe muore al 100% se becca una pipe con binding NAT CGNAT Starlink degradato, dove il round-robin ne assorbiva 1/12 (l'ARQ copriva il buco). E l'upstream NON aveva il problema del riordino (era gia' sano col RR). Configurazione finale **asimmetrica**: `stripe_flow_affinity: true` SOLO nel mp1.yaml del **server** (downstream = la direzione malata), client resta RR. Risultato: P1 -R 43-72 Mbit, P1 up 13-21 Mbit, P30 -R 153 Mbit, tutto inoltrato da pclab.
- **Contorno della stessa notte**: (1) soak 2h07 completato sul sistema pre-affinity (k6 50 VU: pagine 90% ok contro l'8% del pomeriggio, light 99%, routing a zero difetti: rule 6+2 integre, 0 restart, 0 EPERM); (2) guasto lungo di mp1 (4 min, watchdog in pausa): continuita' utente perfetta (ping 0% loss, IP pubblico flippato sul fisico 150.228.25.211 e rientrato sul VPS al recovery) MA ottenuta da un fall-through di bd1 verso main (BOND1 restava "online" finto su mwan3): chiuso con il pavimento blackhole anche su `bd1` (27-bd1.network, client + repo) — ora il fallback fisico passa esplicitamente da `STARLINK_PHY`; (3) resta aperto il comportamento dell'ARQ che ritransmette range enormi per pochi NACK (67k retx per 78 NACK in un run): non blocca (la banda lo assorbe) ma e' il prossimo candidato di tuning.
- **Esito / stato**: fix deployato in configurazione asimmetrica e **validato**. A linea libera, inoltrato da pclab: P1 -R 43-85 Mbit (era 0.3-21), P1 up 13-21, P30 -R 153-187. Sotto saturazione k6 50 VU (validazione 30 min): il collo diventa la capacita' del link condiviso (probe 1.5-29 Mbit in contesa con 50 utenti, ping ~25-35% loss da code piene, k6 pagine 83%/light 94%/mediana 775ms — nel rumore rispetto al pre-affinity: sotto saturazione comanda la banda, non il riordino), routing perfetto per tutta la notte (rule 6+2 integre, 0 restart, 0 EPERM). Prossimi tuning individuati: ARQ range-retx (67k retx per 78 NACK), qdisc/AQM contro il bufferbloat sotto saturazione. **Dual-path validato il 28/07 mattina** (LTE tornato): peer ping 0%, P1 down 33.7/108 Mbit (flusso sul path della sua pipe), **upload bondato 28.9 Mbit** (50/50 sui due canali), **P30 213 Mbit** con ripartizione capacity-proportional spontanea (345 MB Starlink + 80 MB LTE): il bonding si auto-pesa via TCP per-flusso, senza scheduler esplicito. Wan5 riagganciato DA SOLO al ritorno del lease (rule 1205 e host route dal timer di riconciliazione). Test indipendenti dell'utente superati → **tag `v5.1`** su `feat/flow-affinity-3c03829`. Nota dev: semantica del contatore `arq_retx_recv` da rivedere (cresce sproporzionato a NACK e byte sul filo). Raffinamento futuro: hashing pesato sulla capacita' dei path (oggi un flusso singolo puo' pescare una pipe LTE e andare a velocita' LTE).
- **Lezione → wiki**: candidata a valle della validazione — (1) su un trasporto multi-pipe, l'affinity di flusso cura il riordino ma eredita la salute della singola pipe: va messa SOLO nella direzione dove il riordino fa danni, e il RR resta prezioso dove serve resilienza alle pipe degradate (CGNAT); (2) un fallback che "funziona" senza che il sistema di failover se ne accorga e' un fall-through, non un fallback: ogni tabella di policy routing deve avere il suo pavimento.

## TS-023 — Banco TBOX-EVO — Il collaudo passava dal fisico: bypass rimosso, VLAN phy, rollback pre-CAL e caccia di Pollicino al single-stream

- **Target**: client VM 200 + VPS lab `104.105.82.146` + OpenWrt VM 100. Scoperta dell'utente in serata: l'IP pubblico di uscita da pclab era quello Starlink, non quello del VPS.
- **Data**: 2026-07-27 sera / 2026-07-28 notte
- **Concause accertate**: (1) bypass TS-017 con default attivo nello script (`${MPQUIC_BYPASS_WANS:-enp7s8}`): ogni esecuzione fuori dalla unit lo innescava in silenzio: per mesi il percorso "STARLINK" è stato fisico nudo, mai tunnel; (2) host route /32 verso l'IP pubblico del VPS che dirotta sul fisico anche i test iperf (vizio di metodo dei collaudi); (3) policy mwan3 LAN/default su FAILOVER→STARLINK→mpq6 (QUIC datagram, debole: 3-6 Mbit a P30 inoltrato, MSS clamp provato e ininfluente) invece che su BONDING→mp1/STRIPES.
- **Interventi**: bypass RIMOSSO (commit `93a8e29`, acid test: run manuale senza env → mai gw fisico); VLAN dedicate ai canali fisici 95/96 stile BOND1 con tabelle phy5/phy6, rule 1095/1096 networkd-owned, floor blackhole, e `LTE_PHY`/`STARLINK_PHY` membri mwan3 di riserva (l'unico posto legittimo per il gw fisico, scelta esplicita di mwan3); snapshot integrale su branch `debug/anomalia-mpquic-20260727` (`e6ddff7`) e rollback binario a `3c03829` (ultimo commit pre Fase A del CAL/External Crypto Provider, md5 `4c125555` identico su client e VPS); policy LAN/CREW/default riportate su BONDING con PHY in coda.
- **Misure oneste post-rollback (metodo corretto: iperf verso IP di TUNNEL)**: dal client via mp1: P30 -R 137 Mbit, P1 up 17-20 Mbit, P1 -R 0.37 Mbit; da pclab via mp1 (k6 spento): P30 -R 80.6 Mbit, P1 -R 0.31, P1 up 0.10; inoltrato via mpq6: P30 3-6 Mbit. Trace di validazione tunnel: 30 In/30 Out su `enp7s2` e su `mpq6`, zero pacchetti in chiaro sul fisico.
- **Caccia di Pollicino (single-stream)**: discriminante = P1 -R collassa ANCHE dal client (368 Kbit) → OpenWrt/firewall scagionati. Conteggio pacchetti dati su mp1 durante un P1 -R: VPS TX **22.771**, client RX **22.770** (perdita 0.004%), fuori-sequenza al client **22.767 su 22.770 (~100%)**. Nessun filtro: e' il round-robin per-pacchetto sulle 12 pipe senza resequencing al RX. E' la stessa causa radice di TS-013; il reseq che la curava e' stato revertito per la regressione d'aggregato TS-016. Nota di misura: run con variabilita' selvaggia (0.3→27.9 Mbit tra tentativi identici), coerente con cwnd in balia del riordino.
- **Esito / stato**: baseline pre-CAL stabile e allineata sui due lati; routing fail-closed e fallback fisico esplicito via mwan3 validati. **Aperto, decisione di trasporto**: il single-stream inoltrato resta inutilizzabile (0.1-0.4 Mbit) su QUALUNQUE versione senza riordino al RX — serve un reseq corretto (che non ripeta TS-016) o un'alternativa (flow-affinity per-pipe, cap ~80 Mbit), da progettare sul branch di debug. Handoff tbox resta sospeso fino alla decisione.
- **Lezione → wiki**: gia' coperta in parte (TS-013, TS-021 erratum); da aggiungere a valle della decisione di trasporto.

## TS-021 — Banco TBOX-EVO dual-WAN — Rule di policy routing spazzate da networkd: storm EPERM→reconnect→TUN ricreato, e la vera ragione delle WAN "Online" fantasma

- **Target**: banco TBOX-EVO riallineato allo scenario IBLEA-M: client mpquic VM 200 `10.10.11.100` (MAC `bc:24:11:3d:9b:59`, verificata su Proxmox `qm list`), OpenWrt VM 100 `10.10.11.254`, VPS lab `104.105.82.146`. WAN6=`enp7s8` STARLINK (oggi antenna diretta CGNAT 100.64/10), WAN5=`enp7s7` LTE. Attivi mpq5, mpq6, mp1 + classi cr/br/df 5-6.
- **Data**: 2026-07-27
- **Sintomo**: LuCI MultiWAN mostrava Online tutte le WAN comprese quelle con le porte fisiche staccate (WAN1, KVHTS, ONEWEB). Sul client le `ip rule` 1001-1006 risultavano assenti a ogni controllo nonostante `mpquic-routing.service` girasse con successo.
- **Diagnosi (read-only + esperimento controllato)**: due strati. (1) Il verde di LuCI non può venire dal link: su Proxmox il carrier VirtIO non cade mai (TS-001), decide solo il tracking mwan3. (2) I track ping passavano per tutte le WAN perché i `/30` cadevano in `main` e uscivano NATtati dal default fisico: bypass da rule assenti. Esperimento delle 11:22: rule ricreate (6/6), sparite tutte insieme tra 11:23:44 e 11:24:14, in correlazione con 13 eventi `mpq6: Configuring` di networkd in 67 secondi. Journal `mpquic@br6`: `ERROR reconnect in 3s: sendmsg: operation not permitted` alle 11:24:01/09, ogni reconnect riapre il TUN (`TUN br6 opened`), il dispatcher segnala `Interface "mpq6" not found` e `Unknown index seen, reloading interface list` (indici 180→198: TUN distrutti e ricreati). Catena completa: lo script cancellava le rule remote 1201-1206 in testa al run e le riaggiungeva in coda → finestra di secondi → EPERM sui socket QUIC attivi → reconnect → TUN ricreato → networkd riconfigura il link nuovo e, con `ManageForeignRoutingPolicyRules` al default `yes`, spazza le rule foreign (sopravvive solo la 1017 di bd1, networkd-owned: è la prova) → altri EPERM → storm che si autoalimenta fino allo stato stabile sbagliato (zero rule, tutto in bypass). Scoperto inoltre: (a) il `[Route]` nei `5x-mpqN.network` (aggiunto il 24/7) creava doppie default nelle tabelle wanN e, a TUN sparito, lasciava la tabella senza default → fallthrough su `main` = bypass (provato con `ip route get from 172.16.6.2 iif enp7s2` → `via 192.168.1.1 dev enp7s7`: STARLINK che esce dall'LTE); (b) networkd-dispatcher perde eventi di TUN creati in rapida sequenza → il refresh event-driven da solo non basta; (c) sul banco giravano TRE versioni divergenti dello script di routing (repo, lab riscritta a mano il 24/7 che regrediva TS-014 e mancava di TS-011/TS-017, IBLEA pre-TS-020).
- **Fix applicati** (commit sul branch `ottavionovella`, deploy via scp senza tracce repo sulla VM): (1) `8cfa38d` wan rule statiche add-if-absent (port del fix TS-020 nel repo); (2) `be5b903` `replace_prio_rule`: le rule DHCP-dipendenti si toccano solo se cambiate davvero, del+add adiacenti (finestra sub-ms); (3) rule 1001-1006 dichiarate `[RoutingPolicyRule]` nei `2x-lanN.network` → networkd-owned, `proto static`, dal boot, immuni allo sweep + `ManageForeignRoutingPolicyRules=no` in `networkd.conf` come cintura; (4) pavimento `blackhole default metric 9999` per tabella wanN nei `2x-lanN.network` e rimozione del `[Route]` dai `5x-mpqN.network`: fail-closed, mai fallthrough su main; (5) `d335a64` if-event: eventi dei TUN → refresh del routing (prima usciva con `exit 0` per ogni interfaccia non-WAN); (6) `5f34f23` `mpquic-routing.timer` ogni 30s (riconciliazione per gli eventi persi; `RemainAfterExit=no` obbligatorio, con `yes` il timer non rieseguirebbe mai la unit). OpenWrt: LTE→`eth12`/172.16.5.2, ONEWEB→`eth11`/172.16.4.2, chilli `dhcpif` `eth0.5`→`eth0` (rete CP già 192.168.182.0/24). Unit wan-watchdog: `Environment="WAN_INTERFACES=enp7s7 enp7s8"` con virgolette (senza, systemd scarta la seconda interfaccia: "Invalid environment assignment").
- **Validazione (numeri)**: rule 6+2 stabili sotto il trigger che prima le uccideva (campioni ogni 30s per 3 min); contraccolpo del restart networkd riassorbito in 2s senza spirale (EPERM=4 una tantum, poi 0); quiete: 0 restart istanze, 0 EPERM, 0 Configuring in 5 min, ifindex mpq6 fermo; stop `mpquic@6`: `route get` dal /30 → EINVAL (blackhole) e mp1 10/10 0% loss con host route wan6 presente (TS-014 rivalidato); start: default `dev mpq6` reinstallato ≤45s (timer) e peer ping 3/3; da OpenWrt `mwan3 use` STARLINK/LTE/BOND1 → 0% loss (8/8 ciascuno), WAN1/KVHTS/ONEWEB **offline** (rosse), TELESAT disabled. Nota di metodo: `ping -I ethX` da OpenWrt dava un falso 80% loss (interferenza dei fwmark mwan3), il test valido è `mwan3 use <WAN> ping`.
- **Esito / stato**: **chiuso e validato sul banco** per la parte routing/stabilità, incluso il soak di 2h07 con 50 VU k6 (report: [REPORT_COLLAUDO_BANCO_20260727.md](REPORT_COLLAUDO_BANCO_20260727.md)): rule 6/6 in ogni campione, 0 restart automatici, 0 EPERM, 456/456 peer check a 0%, guasto provocato auto-riparato in 10-20s dal tunnel-watchdog. **ERRATUM serale**: i numeri iperf (250-300 Mbit) misuravano il canale fisico, non STRIPES — il target era l'IP pubblico del VPS, servito dalla host route /32 diretta sul fisico; inoltre il bypass TS-017 aveva un default attivo (`enp7s8`) che si innescava a ogni esecuzione dello script fuori dalla unit. Meccanismo bypass RIMOSSO (commit `93a8e29`, acid test: run manuale senza env → wan6 resta dev mpq6/blackhole); throughput STRIPES da rimisurare col metodo corretto (iperf verso 10.200.N.254 o verifica dell'IP pubblico di uscita). VPS lab raggiunto (alias `vps-mpquic-it-tpz-lab`): binario `4ac2bed3` identico su client lab, VPS lab e VPS IBLEA = baseline per la remediation. Aperti, non bloccanti: il client Go ricrea il TUN a ogni reconnect (amplificatore dello storm, valutare fix nel codice: riusare il TUN esistente); attribuzione dei fallimenti k6 "page" (firma da rate-limiting dei target, due prove proposte nel report); failover mwan3 sotto carico non esercitato (il watchdog ripara prima).
- **Lezione → wiki**: candidata, doppia. (1) Su systemd-networkd, le ip rule di policy routing gestite da script sono "foreign" e il default `ManageForeignRoutingPolicyRules=yes` le spazza a ogni riconfigurazione di un qualunque link: le rule statiche vanno dichiarate nei `.network` (networkd-owned) e la dinamica va tenuta idempotente e atomica; ogni tabella custom vuole un pavimento blackhole a metrica alta per fallire chiusa invece che in bypass. (2) networkd-dispatcher perde eventi su create/delete rapidi di interfacce: l'event-driven va sempre accoppiato a una riconciliazione periodica idempotente.

## TS-020 — Lab template (client VM 200 + VPS lab) — Bonifica del template: bypass routing, STARLINK flapping, addressing, IP di IBLEA ereditato

- **Target**: client mpquic VM 200 `10.10.11.100` (satcom, accesso diretto in lab, MAC `bc:24:11:3d:9b:59`), OpenWrt `10.10.11.254`, VPS lab `104.105.82.146`. Da preparare come template per la clonazione delle prossime navi.
- **Data**: 2026-07-24
- **Sintomo**: su OpenWrt tutte le WAN risultavano "online" ma il traffico non passava dai tunnel giusti. Con un tunnel giù, il traffico della sua subnet finiva su `enp7s8` invece di essere scartato. STARLINK (mpq6) andava e veniva. Nel `global.env` della VPS lab c'era ancora l'IP di IBLEA.
- **Diagnosi (read-only prima del fix)**: il metodo di verifica è quello dell'utente: ping fisso su `9.9.9.9` (IP non usato per il tracking) e trace del percorso sul client con tcpdump per interfaccia, non lo stato "online" di mwan3 che è fuorviante. Traccia iniziale: sia STARLINK sia WAN1 uscivano su `enp7s8` in bypass. Le `ip rule` 1001-1006 risultavano sparite, restava solo la 1017 di mp1. Il watchdog (`OnUnitActiveSec=15s`) riavvia in continuazione mpq1-5 (WAN `enp7s3-7` senza IP, morte): ogni evento richiama `mpquic-policy-routing.sh`, che cancella e riaggiunge le `ip rule`. La wan rule (`from 172.16.N.0/30 lookup wanN`) è statica, ma veniva cancellata a ogni giro e restava assente nella finestra tra `del` e `add`; con il churn del watchdog la finestra era quasi sempre aperta, quindi il `/30` cadeva nella tabella `main` e usciva sul default `enp7s8`.
- **Evidenze**: trace STARLINK sotto watchdog attivo dopo il fix: 11 pacchetti `mpq6 In` + 11 `mpq6 Out`, 0 su `enp7s8`, ping 0% loss. Trace BOND1: `enp6s20.17 In` src `172.16.17.2` → `mp1 Out` src `10.200.17.1` → reply → `enp6s20.17 Out`, 0% loss. Conteggio wan rule dopo 30s di churn: 6 presenti (stabili), `wan1` = `blackhole default`. mpq6 flapping: 0 restart in 40s dopo il fix di addressing. VPS lab: `eth0=104.105.82.146/24`, egress reale `104.105.82.146`, boot 2026-07-24 09:26 (il clone), distinta da IBLEA `172.238.232.223` (macchina diversa, verificato all'host prima di toccare il `global.env`).
- **Causa radice**: (1) routing: il `del`+`add` di una regola statica a ogni evento di rete crea una finestra di bypass; con un watchdog che va in loop su tunnel morti la finestra non si chiude mai. (2) flapping: addressing `/30` con peer `.2` invece di `/24` con peer `.254`. (3) IP: il clone della VPS ha ereditato il `global.env` di IBLEA senza riparametrizzazione per-host (stessa famiglia di TS-019).
- **Fix applicate**: `mpquic-policy-routing.sh` reso idempotente: la wan rule non viene più cancellata (è statica) e viene aggiunta solo se assente (`ip rule show | grep -q "^${prio}:" || ip rule add ...`); restano `del`+`add` solo per le rule src/remote che dipendono dal DHCP. Watchdog riabilitato e verificato che le rule reggono sotto churn. Addressing `/30`→`/24` con peer `.254` su mpq1-6 (client `.env` + `.yaml.tpl`, backup `.bak-addr`). VPS lab `global.env` `172.238.232.223`→`104.105.82.146` (backup `.bak-ibleaip-20260724`); rimossi gli `/run/mpquic/*.yaml` stale col vecchio IP lab `172.232.209.51` (istanze ferme, si rigenerano dal template al restart); nel repo `deploy/config/client/mp1.yaml` l'IP hardcoded sostituito dal placeholder `VPS_PUBLIC_IP` come tutti gli altri client config.
- **Esito / stato**: **chiuso e verificato** su STARLINK→mpq6 e BOND1→mp1. **Aperto**: il failover di mp1 `enp7s8`→`enp7s7` chiesto dall'utente. mp1 usa transport stripe, che stripa round-robin sulle pipe; aggiungere `enp7s7` come secondo path group in modo naïve riporta il bug di TS-016 (50% loss quando l'upstream del secondo path è morto). Il codice ha una policy `failover` (score `priority*1000 + fail*100`, path a score minore preferito, il primario perde solo quando accumula fail) ma va verificato che valga tra path group stripe e non degeneri in active-active, poi testato sul campo prima del deploy. Le occorrenze di `172.238.232.223` nel repo (docs ARCHITETTURA/TDD/ROADMAP/NOTA_TECNICA e `deploy/config/global.env`) sono di produzione IBLEA e restano corrette: non vanno bonificate.
- **Lezione → wiki**: candidate — una regola di routing statica non si cancella e riaggiunge a ogni evento, si rende idempotente (add-if-absent); il `del`+`add` va tenuto solo per ciò che cambia davvero (le sorgenti DHCP). Un watchdog che riavvia risorse permanentemente morte amplifica qualsiasi finestra di riconfigurazione a suo periodo. Cross-link a [[deploy-infrastruttura-operazioni]], TS-014 (stessa `ip route replace` idempotente sul lato route) e TS-019 (clone di config senza param per-host).

## TS-019 — TBOX-EVO (lab) — Tunnel che muoiono: VPS di IBLEA ereditata per propagazione config; ML-KEM scagionato

- **Target**: client mpquic lab `10.10.11.100` (satcom, accesso diretto in lab), OpenWrt TBOX-EVO `10.10.11.254`. VPS attesa: `vps-mpquic-it-tpz-lab` 172.232.209.51.
- **Data**: 2026-07-23
- **Sintomo**: i tunnel di EVO muoiono in continuazione. Sospetto utente: conflitto VPS con IBLEA; timore che ML-KEM abbia introdotto bug non testati che si propagano in operatività.
- **Diagnosi**: `global.env` di EVO aveva `VPS_PUBLIC_IP=172.238.232.223` (VPS di IBLEA) e `mp1.yaml`/`mp1_RS_ADAPTIVE.yaml` avevano lo stesso IP hardcoded (non usano il placeholder). Log mp1: `stripe key exchange failed`/`session timeout (no rx 31s)` su wan6. `enp7s8=9.246.8.61` identico a IBLEA → sulla stessa VPS due client con la stessa sorgente si pestano. La VPS lab 172.232.209.51 è irraggiungibile (spenta) da ovunque.
- **Evidenze**: `.bak-ts018` su EVO ha ctime = oggi 14:08 (mtime 28-feb preservato da `cp -a`) → una propagazione config IBLEA→EVO avvenuta oggi. `mpquic-update.sh` fa solo `git pull` del binario, NON sincronizza le config → la propagazione è fuori dal deploy ufficiale, senza parametrizzazione per-host. Audit: i TUN CIDR di EVO (10.200.3-6, 14-16) sono corretti, non ereditati da IBLEA (10.200.17) → il danno era mirato alla VPS. Test crypto locale (relay UDP con loss+RTT 240ms): X25519MLKEM768 e X25519 entrambi 15/15 fino al 30% di loss (ML-KEM +50-70ms trascurabili) → **l'handshake ML-KEM non fallisce più su loss**.
- **Causa radice**: config host-specific (VPS) propagata tra unità senza parametrizzazione. Il phantom di IBLEA (TS-018) NON è presente sull'active di EVO (già fixato oggi). La crypto ML-KEM è pulita.
- **Fix applicate**: `VPS_PUBLIC_IP` → 172.232.209.51 in global.env (backup `.bak-vpsfix`); rimosso l'IP IBLEA hardcoded in `mp1.yaml`/`mp1_RS_ADAPTIVE.yaml` (backup `.bak-vpsfix`); restart istanze. Verificato: 0 connessioni residue verso la VPS di IBLEA → EVO non disturba più la produzione IBLEA.
- **Esito / stato**: **fix VPS chiuso**; verifica mapping/dataloss di EVO **aperta**, attende l'accensione della VPS lab. Il "perché adesso" (richiesto dall'utente) si spiega senza la crypto: (1) install di campo fuori standard, (2) propagazione config senza param per-host, (3) regressione data-plane reseq (TS-016). In lab funzionava perché isolato e pulito.
- **Lezione → wiki**: candidate — (1) la propagazione/clone di config tra unità senza parametrizzazione per-host (VPS, IP sorgente) fa sì che il config di un'unità rompa un'altra e crei conflitti sul VPS condiviso; le config host-specific vanno tenute fuori dai sync o parametrizzate; (2) prima di attribuire guasti operativi alla crittografia PQ, testare: l'handshake ibrido ML-KEM su QUIC è robusto alla loss (QUIC ritrasmette gli Initial), non è un moltiplicatore di fallimenti su link satellitari; (3) Go 1.24 rende X25519MLKEM768 il default TLS — se le `CurvePreferences` non sono pinnate ogni handshake usa ML-KEM by default (fatto, non bug). Cross-link a [[deploy-infrastruttura-operazioni]] e TS-018.

## TS-018 — IBLEA-M — Dataloss 40% del traffico inoltrato da OpenWrt: default fantasma verso OPNsense dismessa + mismatch mapping STARLINK

- **Target**: client VM 200 (mpquic). Accesso: `ssh -J vps-mpquic-it-tpz-iblea satcom@10.200.17.1` (passa da mp1). OpenWrt: `ssh -J vps-mpquic-it-tpz-iblea,satcom@10.200.17.1 root@10.10.10.254` (intermittente).
- **Data**: 2026-07-23
- **Sintomo**: da OpenWrt `ping 8.8.8.8` perde ~40%; dal client sul fisico `ping -I enp7s8 8.8.8.8` perde 0%. Criterio di chiusura del cliente: i due ping devono avere lo stesso loss.
- **Diagnosi**: la causa era già individuata dall'handoff read-only del team tbox. Verificato dal vivo: default `via 10.10.11.1 dev enp6s18 proto static` metric 0, ARP `INCOMPLETE` (VM OPNsense dismessa). Tutto ciò che cade in `main` finisce nel buco nero. Verifica end-to-end da OpenWrt: `ip route get 8.8.8.8 → via 172.16.1.1 dev eth8 src 172.16.1.2`, cioè STARLINK di OpenWrt esce sul /30 `172.16.1.x` = tabella `wan1` del client, che ha `enp7s3` senza IP → blackhole, con failover mwan3 su un satellite lossy (30% loss, RTT 565ms). La Starlink fisica è su `enp7s8`.
- **Evidenze**: baseline `ping -I enp7s8` = 0%, `ping -I enp7s7` = 0% (entrambi vivi dopo rotazione DHCP). Post-fix, ping paralleli forniti dal cliente: OpenWrt 7% loss (386/418, RTT 55.8ms), client fisico 0% (416/416, RTT 29.9ms). Tunnel mp1 (STRIPES): `ping -I 172.16.17.1 8.8.8.8` = **70% loss** (6/20). DHCP di enp7s8 osservato ruotare in pochi minuti: 192.168.1.106 → 9.246.8.61, gateway tra 9.246.8.1 e 192.168.1.1.
- **Causa radice**: `Gateway=10.10.11.1` in `/etc/systemd/network/01-mgmt1.network` (interfaccia di management che non dovrebbe fornire un default gateway) → default morta metric 0 che vince su ogni uscita fisica reale. Secondaria: mapping mwan3 fuori standard (STARLINK sul /30 che il client non porta su enp7s8).
- **Fix applicate**: (1) rimosso `Gateway=10.10.11.1` dalla config (+backup `.bak-ts018`), `networkctl reload`, flush della rotta viva — **persistente**. (2) `ip route replace default via <gw-enp7s8> dev enp7s8 table wan1` — forward e return verificati (return a `172.16.1.2` via enp6s20, direttamente connesso), 0% via enp7s8 — **runtime, non persistito**.
- **Esito / stato**: **CHIUSO lato client/mpquic.** Dataloss 40%→7%, fisico 0%. Il 7% residuo e l'accesso ballerino a OpenWrt sono il substrato instabile dell'install non standard: split Starlink (DHCP che ruota → route wan1 fragile) e VLAN 1 flat (loop L2 → mgmt 10.10.10.254 intermittente). MPQUIC non ripristinabile ora (tunnel mp1 al 70%). Passato al team tbox con il registro [`/opt/TPZ/src/HANDOFF.md`](../../HANDOFF.md) → team tbox per ripristino accessi TBOX (SSH+HTTPS) e install (antenna diretta + VLAN). Aperto lato mpquic: decidere se persistere `wan1→enp7s8` (workaround fragile) o rimappare pulito post-install.
- **Lezione → wiki**: candidate — un `Gateway=` su un'interfaccia di management in systemd-networkd genera una default metric 0 che vince su ogni uplink DHCP reale (metric 105/106) e diventa un buco nero silenzioso quando quel gateway sparisce; le default delle interfacce di management vanno rimosse. Complementare: su un substrato con split (DHCP che ruota) e VLAN flat (loop L2), i fix di routing lato client mitigano ma non stabilizzano — la fix vera è l'install. Cross-link a [[deploy-infrastruttura-operazioni]] e alla lezione return-route per-/30 di TS-011/TS-012.

## TS-016 — IBLEA-M — Crollo prestazioni STRIPES: regressione da reseq (5e74c1e), revert git-pulito e validazione vs tetto fisico

- **Target**: data plane STRIPES, tunnel mp1 (client VM 200 + VPS). Rischio penali contrattuali; riferimento cliente: `iperf3 -P30 -R` da OpenWrt verso il VPS = 266 Mbit.
- **Data**: 2026-07-23 notte
- **Sintomo**: aggregato -P30 -R via mp1 decade da ~125 Mbit a ~40 Kbit in 17s (riprodotto: 113 → 18.6 → 1.2 → 0.35 Mbit su 20s). Decadimento progressivo, non collasso immediato.
- **Diagnosi A/B (tutte le misure nella stessa finestra notturna)**:
  - Kill-switch `stripe_reseq_disable: true` sul solo client: il decadimento RESTA (182→25→0.3; 85→9→0.5) → il buffer reseq lato RX client non basta a spiegare da solo; la regressione sta nel binario (reseq introduce anche modifiche a `stripe_client.go`/`config.go`/`stripe.go`, e c'è `f6c1d6b` su `stripe_server.go`).
  - Binario known-good = commit `374e3a1` (unici delta successivi su cmd/mpquic: `f6c1d6b` + `5e74c1e`): deploy d'emergenza via scp su mp1 (entrambi i lati) → `-t30 -P30 -R`: 105/104/158/200/250/183 = **167 Mbit medi, trend in salita, zero decay**. Regressione CONFERMATA nei due commit.
  - Trappola di misura scoperta: un server iperf3 con sessioni appese ("Resource temporarily unavailable") produce un finto pattern di decadimento — un run post-fix è "collassato" per questo; col server pulito il pattern sparisce. Mai diagnosticare senza riavviare il server iperf3.
- **Fix (git-pulito, best practice deploy)**: `git revert` di `5e74c1e` e `f6c1d6b` sul branch `feat/crypto-abstraction-layer` (commit `2330b4b` + `a7e0f8b`); `git diff 374e3a1 HEAD -- cmd/ internal/ local-quic-go/` = vuoto (equivalenza provata). Push su GitHub non disponibile dall'ambiente → sync dei repo remoti via **git bundle** (/tmp/ts016.bundle) + `MPQUIC_UPDATE_SKIP_PULL=1 mpquic-update.sh`: VPS standard (11 istanze OK), client in modalità connectivity-safe (update detached via systemd-run + dead-man switch; fix necessari: `git config --system safe.directory` e `--setenv=HOME=/root` per la module cache Go sotto systemd-run). **Il binario buildato via git sul client ha lo stesso md5 del known-good buildato sul dev box (cb5eb0d0…): build riproducibile, equivalenza totale.** La deroga scp iniziale è stata riassorbita dal percorso git.
- **Validazione finale (Verification Gate, numeri)**:
  - Client VM `-t30 -P30 -R` via mp1: **199 Mbit medi, picchi 268**, sostenuto.
  - OpenWrt (path esatto del cliente) `-t30 -P30 -R`: **173 Mbit medi** (190/170/160), sostenuto, zero decay.
  - Tetto fisico nello stesso momento (diretto, no tunnel, -P30 -R): **294 Mbit** → efficienza tunnel 59-68% con FEC adattivo M=2 attivo (20% overhead parity sotto loss).
  - Single-stream DL via mp1: 20.3 Mbit (il collasso TS-013 non si ripresenta nelle condizioni attuali senza reseq).
  - Uplink via tunnel: 4-6.6 Mbit vs 46.9 diretto — limite PREESISTENTE del trasporto in salita (nessun riordino lato server RX), non toccato dalla regressione; in carico al piano trasporto.
- **Verdetto sul target 266**: il decadimento a zero è eliminato; il 266 non è raggiungibile stanotte perché il tetto fisico era 294 (servirebbe efficienza 90%, impossibile con FEC M=2). Col tetto Starlink diurno (350-400+) l'efficienza attuale 60-70% rende il 266 realistico. Da far rimisurare al cliente in finestra diurna; se non basta, stadio 1 del piano trasporto (pacing server, tuning FEC/pipes/interleave) in finestra di manutenzione concordata.
- **Stato**: **CHIUSO** lato regressione (revert deployato e validato entrambi i lati). Aperti: conferma cliente sul target in finestra buona; piano trasporto stadio 1/2 (include uplink e single-stream robusto al posto del reseq). Nota: `stripe_reseq_disable: true` resta nella config client mp1 (chiave ignorata dal binario attuale, protezione se mai tornasse un binario con reseq). Backup fuorvianti da ricordare: `mpquic.bak-known-good` su entrambi gli host è il binario REGREDITO (backup pre-fix), non il known-good.
- **Lezione → wiki**: candidate — (1) reorder buffer RX con finestra adattiva può degradare progressivamente l'aggregato multi-stream (HOL cross-flow) pur avendo risolto il single-stream: ogni fix di trasporto va validato su ENTRAMBI i profili (-P1 e -P30, sostenuto ≥30s); (2) misure di throughput: prima di diagnosticare regressioni, riavviare il server iperf3 (sessioni appese producono finti decadimenti); (3) ogni misura in-tunnel va confrontata col tetto fisico diretto misurato nello stesso minuto; (4) deploy git-pulito senza push: git bundle + mpquic-update.sh SKIP_PULL, con safe.directory a livello system e HOME esplicita per systemd-run.

## TS-015 — IBLEA-M — Outage serale: 12/24 pipe di mp1 in blackhole su enp7s7 (router LTE con lease DHCP ma upstream morto)

- **Target**: client mpquic VM 200, config `/etc/mpquic/instances/mp1.yaml`, interfaccia `enp7s7` (router LTE), tunnel mp1 (unico path di bordo dopo lo spostamento del default mwan3 su policy BONDING).
- **Data**: 2026-07-22 sera
- **Sintomo**: il cliente non raggiunge più nulla (ZeroTier "stabile ma non risponde", ssh alle VM impossibile) mentre dal VPS i servizi tunnel risultano tutti `active`. Nota: il ping del cliente a 10.202.11.2 era verso l'IP ZeroTier di OpenWrt stesso (RTT 0.04 ms) — non prova nulla sull'esterno.
- **Diagnosi (read-only, con numeri)**: dal VPS `ping 10.200.17.1` = **50% loss** (10/20) mentre `10.200.6.1` (mpq6, stesso dish) = 0%. Log server: tutte le 12 pipe registrate di mp1 arrivano dallo stesso IP pubblico Starlink 129.222.57.94. Lato client: la config mp1 deployata aveva **2 path group da 12 pipe: `if:enp7s7` + `if:enp7s8`** (24 pipe totali, drift rispetto al template repo `deploy/config/client/mp1.yaml` che era già single-WAN). `enp7s7` aveva lease DHCP attivo (192.168.11.184 via 192.168.11.1) e carrier=1 dal router LTE, ma upstream morto: `ping -I 192.168.11.184 172.238.232.223` = **100% loss** (blackhole), `ping -I 192.168.1.106` (enp7s8) = 0%. Le 12 pipe su enp7s7 non si registrano mai al server ma il TX del client stripa round-robin anche su di loro → **50% teorico = 50% misurato**. `wan_usable()` dello script di routing non può accorgersene: guarda solo carrier+IPv4.
- **Perché proprio ora**: il router LTE ha (ri)dato un lease a enp7s7 — finché l'interfaccia era senza IP il gruppo di pipe non nasceva e mp1 era di fatto single-WAN sano (spiega il "mp1 rende 380 Mbit" storico). L'aggravante: il cliente aveva appena tolto il failover mwan3 (tutto su BONDING→mp1), quindi il 50% di loss è diventato outage totale di bordo.
- **Fix applicato**: config mp1 riallineata al template repo, **single-WAN `if:enp7s8`, 12 pipe** (backup `/etc/mpquic/instances/mp1.yaml.bak-fix7s7`); restart client via `systemd-run` con dead-man switch (revert automatico a 300s se non confermato — il primo tentativo con `setsid`+`&` in sessione `-tt` non è mai partito: SIGHUP, lezione operativa). Restart server mpquic@mp1 per pulire le sessioni stale (residuo 10% loss con `per-session fd dispatch failed`) → **0% loss** post-pulizia.
- **Esito / stato**: **CHIUSO**. Connettività di bordo ripristinata (OpenWrt: ping 8.8.8.8 ok, DNS via 8.8.8.8 risolve attraverso il tunnel; mwan3 BOND1/STARLINK online). Resta la variabilità del downlink Starlink sotto carico (ICMP in-tunnel 6-46% a seconda del momento, link diretto 0% in idle) — è il tema TS-013/analisi transport in corso, non questo incidente. Nota collaterale: `curl` su OpenWrt è rotto di suo (symbol error `curl_easy_ssls_import`, preesistente).
- **Lezione → wiki**: "un canale WAN con DHCP+carrier ma upstream morto supera i check `wan_usable` (carrier+IP) e trasforma ogni pipe bindata lì in un blackhole silenzioso; il liveness va fatto end-to-end (registrazione pipe al server), e il TX non deve stripare su pipe mai registrate". Da valutare ingest in wiki insieme al fix di trasporto (esclusione pipe non registrate dal dispatch TX).

## TS-014 — IBLEA-M — Route host VPS accoppiata al TUN sbagliato: mp1 va a blackhole quando si ferma mpquic@6

- **Target**: client mpquic VM 200 (`satcom@10.200.17.1` via `-J vps-mpquic-it-tpz-iblea`, `-tt`), script `/usr/local/sbin/mpquic-policy-routing.sh` (repo: `scripts/mpquic-policy-routing.sh`), tabella di routing `wan6` (105), hook `networkd-dispatcher`.
- **Data**: 2026-07-22
- **Sintomo**: test sul campo, `systemctl stop mpquic@6` (tunnel STARLINK single-link su `enp7s8`). Le pipe UDP del tunnel `mp1` — bindate come source IP `192.168.1.106` (`enp7s8`) verso il VPS `172.238.232.223`, instradate in `wan6` dalla ip rule 1206 `from 192.168.1.106 to 172.238.232.223 lookup wan6` — muoiono per **18.4s** fino al riavvio manuale di `mpq6`. mp1 non ha nulla a che fare con mpq6 se non la WAN fisica condivisa.
- **Diagnosi**: lo script rigenera le route della tabella `wan6` a ogni evento di rete (hook `networkd-dispatcher`). Il blocco che gestisce sia la host route verso il VPS (`172.238.232.223/32 via 192.168.1.1 dev enp7s8`) sia il default del tunnel (`default dev mpq6` / `blackhole default`) era condizionato dallo stesso `if wan_usable "$dev" && have_tun_up "$tun"`. Fermando `mpquic@6`, `have_tun_up(mpq6)` torna false, lo script rientra nel branch else e cancella **sia** il default (giustamente, sostituendolo con blackhole) **sia** la host route del VPS in `wan6` — che però è l'unica via per cui le pipe di mp1 raggiungono il VPS, perché quella tabella è selezionata dalla ip rule per source+dest specifici, non dal tunnel mpqN.
- **Evidenze**: blackout misurato di 18.4s sulle pipe mp1 durante lo stop di mpquic@6, rientrato solo al riavvio di mpq6 (che ripristina `have_tun_up` e quindi la route). File repo trovato disallineato dal deployato: mancava la funzione `lan_dev_for_subnet()` e il blocco di route `/30` specifica verso il router (fix separato, già in produzione, non documentato nel repo — vedi nota sotto).
- **Causa radice**: la tabella `wan6` è una risorsa per-WAN (una per `enp7s8`), condivisa da qualunque tunnel instradato lì tramite ip rule su source/dest — non è proprietà esclusiva di `mpq6`. Accoppiare la host route del VPS allo stato del TUN mpq6 rompeva questa condivisione: un tunnel "ospite" (mp1) perdeva la via verso il VPS ogni volta che il tunnel "titolare" della tabella (mpq6) andava giù, anche solo per un restart controllato.
- **Fix applicato**: separate le due decisioni nello script. La host route (`${rip}/32 via $gw dev $dev`) ora dipende solo da `wan_usable "$dev"` — resta finché la WAN fisica è su, indipendentemente dal TUN. Il default/blackhole resta come prima, condizionato a `wan_usable && have_tun_up` (necessario: serve al traffico LAN steerato nel tunnel, e il blackhole a TUN giù è corretto e voluto). Prima del fix, il repo è stato riallineato al deployato (era indietro di un fix pregresso, il blocco `lan_dev_for_subnet`/route `/30`) per non farlo regredire al prossimo deploy.
- **Esito / stato**: **CHIUSO E VALIDATO SUL CAMPO** (2026-07-22 23:56 CEST). Review approvata, fix deployato sul client (backup `/usr/local/sbin/mpquic-policy-routing.sh.bak-ts014`, md5 verificato). Validazione con test monitorato identico al test di prova (stop `mpquic@6` 60s, monitor 1 Hz su client + ping dal VPS): host route VPS presente in wan6 in **38/38 campioni** durante lo stop (pre-fix: cancellata), max FAIL consecutivi su mp1 = **1** (pre-fix: 14, blackout 18.4s), RTT max dal VPS 126 ms (pre-fix: 10.285 ms per stallo di coda). mp1 durante lo stop di mpq6 mostra solo il rumore ambientale del link. Obiettivo del cliente raggiunto: mpq6 (e mpq5) si possono fermare senza impattare mp1. Debiti tecnici dalla review, non bloccanti: test di non-regressione bats per lo scenario TS-014, `flock` nello script contro run concorrenti.
- **Lezione → wiki**: pattern generalizzabile — "in uno script di policy routing multi-tunnel, non accoppiare mai una risorsa condivisa per-WAN (host route verso l'endpoint remoto) allo stato di un singolo tunnel/TUN che si appoggia su quella WAN come uno fra tanti; separare la condizione sulla risorsa condivisa (solo stato fisico del link) da quella sulla risorsa esclusiva del tunnel (default route/blackhole)". Da valutare per ingest in `wiki/` vista la ricorrenza del pattern "route condivisa vs entità di failure singola" già visto in altri incidenti multi-WAN.

## TS-013 — IBLEA-M — Throughput a pochi stream a fame: reordering da striping per-pacchetto + loss variabile Starlink

- **Target**: client mpquic VM 200 (`satcom@10.200.17.1` via `-J vps-mpquic-it-tpz-iblea`, `-tt`) + server VPS (`vps-mpquic-it-tpz-iblea`, `172.238.232.223`). Data plane STRIPES, tunnel mpq6 (STARLINK single-link, 12 pipe) e mp1 (BOND1). Handoff dal team tbox: registro `/opt/TPZ/src/HANDOFF.md` (→ team mpquic, TS-013).
- **Data**: 2026-07-22
- **Sintomo**: il cliente naviga a fame a pochi stream (speedtest 0.67 Mbps down / 0.44 up) mentre l'aggregato regge (`iperf3 -P30` ~240 Mbit). Il team tbox aveva già escluso QoS OpenWrt, egress VPS (758 Mbit), link fisico (ping 0%), scelta tunnel e MSS clamping.
- **Diagnosi (read-only prima di ogni fix)**: misure end-to-end verso l'IP tunnel del VPS (terminano al VPS, puro trasporto). **Asimmetria netta**: upstream single-stream 40-51 Mbit 0 retx (sano); **downstream single-stream 73 Kbit** con retx (rotto); downstream aggregato -P30 169 Mbit (sano). Letto il codice: sia server (`stripe_server.go` SendDatagram/sendFECGroupLocked) sia client (`stripe_client.go`) distribuiscono ogni pacchetto round-robin su tutte le pipe (`txPipe % len(pipes)`), e il ricevitore consegna in ordine di arrivo (`deliverDataDirect`/`deliverGroupData`) **senza reorder buffer**. Design del fix con transport-expert: reorder buffer al RX keyed sulla sequence per-pacchetto (`GroupSeq+ShardIdx`, contigua in M=0 e M>0), finestra adattiva da `arqRx.maxOOO`, backstop temporale, iniezione recuperi FEC. Deployato il reseq sul client (downstream): con reseq stats live si vede che il reordering c'è davvero (`reordered` >> `inorder`), ma resta un **~38% di gap che non arrivano mai** (`gapskip` alto, `dupold=0`, `fecfilled=0`): loss reale, non riordino. Escluso che sia interno: NIC client `dropped=0` (2.5 Mbit/s ricevuti), kernel client/VPS `RcvbufErrors=0`, CPU client 2.5% load 0.2, tutte le 12 pipe downstream vive e uniformi (~194 pkt), `dispatch_drop/decrypt=0`. ARQ disattivato → downstream **peggiora** (155→75 Kbit) ⇒ la loss è **reale** e il recupero aiuta, non uno storm auto-inflitto. FEC alta (M=4 always) → **peggiora** (aggiunge overhead sul downlink già congestionato). Misura sostenuta 30s: ramp a ~14 Mbit poi un burst Starlink fa collassare cwnd a ~150 Kbit e non recupera.
- **Evidenze**: matrice throughput (up P1 50M / down P1 73K / down P30 169M); reseq `reordered=2709 gapskip=2104 fecfilled=0`; client `arq_dup_filtered=2555` (server retransmette molti duplicati); NIC `dropped=0`; server `adaptive_m=2 loss_rate_pct=100`; post-fix downstream single-stream **~31 Mbit sostenuti su 20s** (da 0.67 Mbps del cliente).
- **Causa radice**: due livelli. (1) **Striping per-pacchetto senza resequencing al RX** → reordering consegnato a TCP → collasso single-stream (mascherato da -P30). Risolvibile nel trasporto. (2) **Loss variabile del downlink Starlink** sotto il traffico del tunnel (esterno al client, non recuperabile dal trasporto con FEC/ARQ senza overhead controproducente). Il #2 domina i momenti brutti.
- **Fix applicate**: nuovo **reorder buffer al ricevitore** (`cmd/mpquic/stripe_reseq.go`, keyed su `GroupSeq+ShardIdx`, finestra adattiva + backstop, recuperi FEC iniettati, nessun cambiamento wire, kill-switch `stripe_reseq_disable`). Wired sul client (downstream). Deployato fleet-wide: tutti i tunnel client (mpq3/5/6, cr/br/df5/6, mp1) e tutti i tunnel server VPS, un tunnel per volta, mp1 (path d'accesso) per ultimo con auto-rollback. Backup binari `.bak-ts013-20260722` / `.bak-predeploy-ts013`. **Scartato**: FEC alta (peggiora), riduzione pipe e flow-affinity (cappano il single-stream a ~80 Mbit, sotto target). Commit `5e74c1e` su `feat/crypto-abstraction-layer`.
- **Esito / stato**: **CHIUSO** lato trasporto. Single-stream downstream da 0.67 Mbps a ~31 Mbit sostenuti (variabile con Starlink), aggregato invariato — il collo di bottiglia residuo è la variabilità del downlink Starlink, fuori dal trasporto. **>200 Mbit costanti single-stream su un solo link Starlink non sono un obiettivo realistico** (Starlink single-flow è intrinsecamente variabile e spesso <200); per banda piena il cliente usa l'aggregato multi-flusso (già ~240). Concordato con l'utente. CT 199 di test ripristinato su VLAN 50. **Follow-up non bloccanti**: metriche reseq via Prometheus; wiring server-side del reseq per l'upstream (oggi sano, quindi bassa priorità).
- **Lezione → wiki**: pattern "aggregato-vs-single-stream su tunnel multipath: lo striping per-pacchetto senza resequencing al RX fa collassare il TCP single-flow, mascherato da -P30; il fix è un reorder buffer keyed sulla sequence per-pacchetto, non ridurre le pipe". Già tracciato in `wiki/tbox/troubleshooting-lessons-flotta.md` (TS-013); da completare a valle con la nota sul limite Starlink single-flow.

## TS-001 — VM ROMARS — Detection DHCP su cable swap: carrier VirtIO fantasma + asimmetria dei lease LTE/Starlink

- **Target**: VM client MPQUIC del fornitore ROMARS, Proxmox ID 300 (hostname `mpquic`, Debian 12 bookworm) — 10.10.11.100 — accesso via `ssh -p 3222 satcom@10.202.15.2` su TBOX-EVO 10.202.15.2. Ha sostituito la nostra VM ID 200, rimossa dalla TBOX-EVO.
- **Data**: 2026-07-16
- **Stato incidente**: **Analisi chiusa**. Cause radice identificate con evidenze, soluzione documentata e consegnata. L'applicazione è a carico di ROMARS: nessuna modifica è stata fatta sulla loro VM, trattandosi di macchina di un fornitore.

- **Sintomo (riportato da ROMARS)**: due problemi distinti, riportati come uno solo.
  1. La VM non si accorge dell'attacca e stacca del cavo Starlink (`enp7s8`) e del cavo LTE (`enp7s7`).
  2. Scambiando fra loro i due cavi, l'IP DHCP dell'LTE non viene rilasciato: la rotta resta installata, il canale sembra online, ma non passa traffico.

- **Diagnosi (read-only)**: raccolta con `ip`, `networkctl status`, `systemctl status`, lettura di `/run/systemd/netif/leases/` e dei contatori in `/sys/class/net/`. Nessuna configurazione modificata, nessun servizio riavviato, nessun modem toccato.
  - Stack di rete: `systemd-networkd` attivo; `NetworkManager`, `networking` e `dhcpcd` inattivi.
  - I file in `/etc/systemd/network/` e `/etc/systemd/system/wan-watchdog.service` sono **byte-identici ai nostri**, `Documentation=https://github.com/peppechiapparo/mpquic` compreso. La VM 300 è nata da un clone della nostra immagine.
  - Cablaggio reale al momento dell'analisi: `enp7s7` = LTE (192.168.1.100/24, gw 192.168.1.1, metrica 105), `enp7s8` = Starlink (9.246.8.61/23, gw 9.246.8.1, metrica 106). I commenti nei `.network` etichettano entrambe come Starlink: etichette nostre ormai fuori allineamento.

- **Evidenze**:
  - **Carrier fantasma.** Gli slot modem vuoti `enp7s3`, `enp7s4`, `enp7s5`, `enp7s6` (nessun modem collegato) riportano tutti `carrier=1 operstate=up`, esattamente come le due WAN cablate. `carrier_changes=2` su **tutte e sei** le interfacce, con VM up da 1h31m (boot 2026-07-16 12:06:33): 2 corrisponde alle sole transizioni di boot, quindi zero variazioni di carrier osservate in tutta la vita della VM.
  - **Asimmetria dei lease** (`/run/systemd/netif/leases/`):
    - LTE, lease `14` (`enp7s7`): `T1=43200` (12h), `T2=75600` (21h), `LIFETIME=86400` (24h), `SERVER_ADDRESS=192.168.1.1`.
    - Starlink, lease `15` (`enp7s8`): `T1=150` (2m30s), `T2=262` (4m22s), `LIFETIME=300` (5m), `SERVER_ADDRESS=9.246.8.1`.
    - Rapporto T1: **288:1**.
  - **Watchdog morto.** `wan-watchdog.service` è `enabled`, ma `/usr/local/bin/` è **vuoto**: `Process: 2869 ExecStart=/usr/local/bin/wan-watchdog.sh (code=exited, status=203/EXEC)`, `Active: activating (auto-restart)`. Con `Restart=always`/`RestartSec=10` ritenta ogni 10s e non ha mai eseguito una riga dal boot.
  - **Timeline dello snapshot.** `.network` datati 11 mar 08:30, `wan-watchdog.service` 11 mar 08:37. Il nostro fix `b498c63` ("wan-watchdog pipefail crash on grep no-match", che sostituisce `set -euo pipefail` con `set -uo pipefail` e aggiunge `|| true` ai due `grep`) è dell'11 mar 08:54, **17 minuti dopo**. Uno script recuperato da copia coeva alla loro immagine conterrebbe ancora quel bug.
  - Al momento del test entrambi i gateway rispondevano (`192.168.1.1` e `9.246.8.1` REACHABLE), cioè cavi in posizione corretta e stato coerente.

- **Causa radice (due cause indipendenti)**:
  1. **Il carrier VirtIO non cade mai.** La NIC della VM è un VirtIO attestato su un bridge dell'host Proxmox; il tap resta up qualunque cosa succeda alla porta fisica. La VM non riceve nessun evento di link, il client DHCP non ha motivo di rifare un DISCOVER e si tiene il lease della rete sbagliata. Qualunque logica basata su link up/down è strutturalmente inutilizzabile su questa piattaforma: gli slot senza cavo si dichiarano attivi come quelli collegati.
  2. **Lease LTE da 24 ore.** Dopo lo scambio dei cavi il lato Starlink si ripara quasi da solo (rinnovo a 150s, rebind a 262s, scadenza a 300s: cinque minuti). Il lato LTE resta congelato: primo tentativo di rinnovo dopo **12 ore**, durante le quali indirizzo e rotta di default metrica 105 restano installati e `networkctl` dichiara `routable (configured)`. Il canale supera ogni controllo di stato locale mentre instrada verso un gateway che su quel cavo non esiste più. È il "sembra online ma non lo è", con finestra di buco nero fino a mezza giornata.
  - **Perché `SendRelease=yes` non salva** (è presente nei `.network`): senza evento di link networkd non ha occasione di iniziare un release; e anche forzando la riconfigurazione, il DHCPRELEASE è unicast al server del lease (192.168.1.1), che dopo lo scambio non è più raggiungibile su quel cavo, quindi il pacchetto si perde. Il binding lato modem sopravvive comunque per le sue 24 ore. Questa parte **non è risolvibile dal client**.

- **Fix proposte** (documento di handover: [ROMARS_WAN_DHCP_Analisi.md](ROMARS_WAN_DHCP_Analisi.md)):
  1. **Copiare `scripts/wan-watchdog.sh` in `/usr/local/bin/`** + `chmod +x` + `systemctl restart wan-watchdog.service`. Non servono `daemon-reload` né `enable`: l'unit è già installata e abilitata. Usare la **versione corrente** dello script, non quella coeva allo snapshot (vedi bug `b498c63`). Impatto: risolve entrambi i sintomi lato client, portando il recupero da 12 ore a circa 60-70 secondi, indipendentemente dalla durata del lease. Rischio: basso, il daemon è in produzione da noi da marzo.
  2. **Abbassare il lease time del modem LTE** da 86400 a 120-300s, allineandolo allo Starlink. Impatto: fa scadere il binding vecchio in minuti invece che in un giorno, evita il consumo del pool su cicli ripetuti di scambio. Rischio: basso; richiede accesso alla web UI del modem (risponde su 192.168.1.1 con sessione Java, gw MAC `60:32:b1:5b:d8:0a`).
  3. Correggere i commenti in `14-wan5.network` e `15-wan6.network`, che chiamano Starlink un'interfaccia che oggi porta l'LTE. Cosmetico ma induce in errore.
  - Le prime due sono complementari e vanno applicate entrambe: il watchdog copre comunque anche con lease lungo perché non dipende dai timer DHCP; il lease corto riduce il problema all'origine.

- **Limiti noti della soluzione** (comunicati a ROMARS):
  - Il watchdog rileva "gateway irraggiungibile", non "modem senza connettività a monte". Modem che risponde ma SIM senza copertura = falso negativo. Stesso pattern del track_ip solo-gateway di TS-001 MATILDE lato tbox (FBB Iridium). Quel caso è coperto dalla path liveness dello strato MPQUIC, non dal watchdog.
  - Due modem sulla stessa sottorete rendono lo scambio non rilevabile (il ping al gateway risponde comunque, ma è il modem sbagliato). Non è il caso attuale, dato che le due reti sono diverse.
  - Il parsing dell'output testuale di `networkctl status` è fragile per costruzione: già costato il fix `b498c63`, da ricontrollare su major upgrade di systemd.

- **Esito / stato**: analisi consegnata. Il punto che sblocca ROMARS è il numero 1: la nostra correzione era già formalmente presente sulla loro macchina ma non ha mai girato, ed è il motivo per cui hanno gli stessi identici sintomi che avevamo noi. Nessuna modifica applicata da noi sulla VM del fornitore.

- **Lezione → wiki**: candidata alla distillazione in `wiki/tbox/troubleshooting-lessons-flotta.md`. Pattern riusabile su tutte le navi con client in VM Proxmox: il carrier delle NIC virtualizzate non è un segnale di stato del cavo, e i lease DHCP lunghi trasformano un cambio di cablaggio in un buco nero che supera ogni health check locale. Si aggancia ai pattern già presenti su lease rubate (Starlink su switch LAN) e track_ip solo-gateway.
